# 2026-09-21 — Mobile Sync Fixes Day

*Morning session ~12:26–13:21 IST; afternoon session ~20:21–21:50 IST. Agent: Cloudy (main); Deepak device-verifying on T42g.*

## Morning — T42g follow-ups (2 commits)

1. `8fafd34` (12:42 IST) — Settings "Sync Now" button (`remoteStorageSettings.ts:377`) passes `useModal: true`; the progress modal now shows from Settings (command palette already had it via `bfd525b`). Auto-sync-on-save intentionally stays modal-less — the status-bar indicator covers the ambient path.
2. `5749148` (13:21 IST) — **critical**: `emitLog` self-recursion in `src/lifecycle/sync.ts` (two helpers, ~L197 rebuild path / ~L409 triggerSync path) called `emitLog(entry)` unconditionally → `RangeError: Maximum call stack size exceeded` on **every** sync op (upload/download/conflict, surfaced via SyncEngine per-session catches :352/:390/:432). Introduced by hub-wiring `e604eed`; fixed to `options?.onLog?.(entry)`. Unit suites missed it (bug in lifecycle wiring, not hub/engine internals; `emit()` swallows listener errors).

## Afternoon — sync/storage correctness fixes (T24 + T48b)

Device report (T42g): synced sessions showed "no messages" until each was opened; later text sends in image conversations demanded an image model.

3. `20ae983` (20:54 IST) — **hydrate retry.** `_hydrateSessionImpl` (ChatStorage) no longer marks a session hydrated on a failed read or when 0 messages arrive against an index expecting N; stays unhydrated, retries next open, logs; partial reads warn. `useSessionActions` silent `.catch(() => {})` → console.error + Notice "Failed to load session messages — will retry on next open". Root cause of permanent-empty synced sessions.
4. `f812c3f` (20:54 IST) — **replay image placeholder.** `buildReplayContent` maps image parts → `[image attached]` text, so old attachments don't force an image-capable model on new text sends. Persisted transcript untouched; genuinely new attachments still send as images.
5. `1fb9263` (21:28 IST) — **picker indexed count.** Preview line hardcoded "No messages" for unhydrated sessions; now renders "N messages — open to view" from the boot-loaded index. Zero file reads. Regression test added (unhydrated fixture, messageCount 3).

## Side finding — generated stylesheet (T66)

`styles.css` is generated by `scripts/concat-styles.mjs` from `styles/_*.css` partials — the recurring "vanishing T42g CSS" was every build clobbering hand-appended CSS (14:29 UTC incident, ~544-line tail loss). At 15:17 UTC an in-session actor (not Cloudy) correctly relocated the T42g block into `styles/_sync.css`. Both files remain uncommitted per Deepak's 15:38 UTC instruction ("don't worry about the css files for now").

## Verification

- Build (tsc + esbuild) green at every step; ChatStorage 12/12; SessionPickerModal 2/2.
- Pushed `5749148..1fb9263` to main.
- Deepak on-device (16:19 UTC): "Good. It works." — existing conversations hydrate on open; history counts accurate.

## Open

- `latest-dev` build from main: cutting now would include the uncommitted CSS work — Deepak's call.
- v1.6.0 release still pending (T42g now unblocked).
- T24a ⏸️ unchanged.
