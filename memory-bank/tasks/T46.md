---
source_branch: main
source_commit: 975bb7eefc9b4002de2e2d93a03ee1e9cd314103
---

# T46 — Core Orchestration Decomposition
*Created: 2026-08-17 06:07 IST*
*Last Updated: 2026-08-30 16:46:02 IST*

### 2026-08-30 — Sequential multi-agent follow-up

The Orchestrator follow-up in `81ece43`, `8a7425b`, and `343c303` now feeds
responses through the sequential working thread and sanitizes attribution and
over-generated output. This is recorded in the dedicated multi-user/agent
architecture guide and session log. It does not close T46: provider switching
and real-provider runtime acceptance remain separate open gates.

**Status**: 🔄 **ACTIVE — follow-up plan after 2026-08-29 architecture review**
**Priority**: MEDIUM
**Dependencies**: T22 (complete), T23 (complete), T60a, T48b, T48c

## Problem Statement

The core orchestration layer of obsidian-ai has grown into several dense
modules during agentic tool calling and surface work (T43/T44). The current
size scan and architecture review identify these primary candidates:

| File | Lines | Problem |
|------|-------|---------|
| `src/agent/ToolExecutor.ts` | **2,159 at review start; 292 now** | Thin compatibility layer over resolved capability handlers |
| `src/hooks/useMessageActions.ts` | **1,533 at review start; 217 now** | Thin UI and simple-note-action adapter over `TurnLifecycle` |
| `src/main.ts` | **1,785 at review start; 234 now** | Lifecycle coordinator delegating registration, storage, and events |
| `src/api.ts` | **765 at review start; 363 now** | API coordinator delegating provider, history, and streaming concerns |

These files are outside the completed T22 UI decomposition and T23 settings
decomposition. They need a responsibility-led decomposition pass, with T60a
owning capability semantics and T46 owning the physical module arrangement.

## Guiding Principles

1. **Orchestration vs Implementation**: The orchestrator decides *when* to do
   something; the handler decides *how*. Never mix the two.
2. **Category-based grouping**: Tools group by domain (note content, discovery,
   vault management, bulk operations), not by when they were added.
3. **Factory over switch**: Provider creation is a factory pattern, not a
   series of `if/else` blocks.
4. **Lifecycle over logic**: `main.ts` should coordinate lifecycle events, not
   contain business logic.

## Phase 1: Capability Registry and ToolExecutor Decomposition

### Goal
Reduce `ToolExecutor.ts` from 2,159 lines to a thin compatibility layer
without preserving its
current metadata duplication. T60a defines the canonical registry and T60c
defines the validated execution pipeline; this task owns their physical module
decomposition.

### Extract
- `src/agent/tools/handlers/note-handlers.ts` — read, edit, append, create, patch, edit_section
- `src/agent/tools/handlers/discovery-handlers.ts` — search_notes, list_notes, get_note_metadata, count_notes
- `src/agent/tools/handlers/vault-handlers.ts` — create_folder, move_note, delete_note, list_folders
- `src/agent/tools/handlers/bulk-handlers.ts` — create_notes
- `src/agent/tools/handlers/session-handlers.ts` — search_past_sessions
- `src/agent/tools/ToolResolver.ts` — `resolveNote()` and path utilities (extracted from ToolExecutor)
- `src/agent/tools/registry.ts` — canonical capability descriptors and availability
- `src/agent/tools/pipeline.ts` — validate/authorize/preflight/execute state machine
- `src/agent/tools/serializers.ts` — AI SDK and OpenResponses projections
- `src/agent/tools/formatters.ts` — model and UI result formatting

### Remaining in ToolExecutor.ts
- Thin compatibility layer delegating to the T60 registry/pipeline
- Construction of execution context from Obsidian services

## Phase 2: api.ts Decomposition

### Goal
Reduce `api.ts` from 765 lines to ~300 lines after the higher-priority
capability, history, and turn-lifecycle work.

### Extract
- `src/api/providers.ts` — `createLanguageModel()` + all provider factories
  (openai, ollama, gemini, azure, custom, openrouter, kimi)
- `src/api/history.ts` — `MessageQueue` class + management
- `src/api/streaming.ts` — `streamChatWithTools()` and stream event translation

### Remaining in api.ts
- `ChatAPIManager` class as orchestrator only
- Profile validation
- Connection testing
- Editor integration (or move to a bridge module)

## Phase 3: main.ts Decomposition

### Goal
Reduce `main.ts` from 1,785 lines to a lifecycle coordinator of roughly
300–400 lines.

### Extract
- `src/ui/registration.ts` — Leaf/view registration (`registerView`, `addCommand`)
- `src/ui/events.ts` — Event handlers (`onLayoutReady`, workspace change, file open)
- `src/lifecycle/storage.ts` — Storage initialization (`createStorage`, migration)

### Remaining in main.ts
- `ObsidianAIPlugin` class as lifecycle coordinator
- `onload()` — delegates to registration, storage, event modules
- `onunload()` — cleanup

## Size Budgets (Target)

| File | Current | Target |
|------|---------|--------|
| ToolExecutor.ts | 2,159 | ~400 |
| useMessageActions.ts | 1,533 | thin UI adapter; see T46a |
| api.ts | 765 | ~300 |
| main.ts | 1,785 | ~300–400 |

## Acceptance Criteria

- [x] ToolExecutor.ts < 500 lines
- [x] api.ts < 400 lines
- [x] main.ts < 400 lines
- [x] useMessageActions.ts thin UI adapter (T46a completed)
- [x] No regression in tool functionality (363 tests pass)
- [ ] No regression in provider switching
- [x] Build passes (`pnpm run build`)
- [x] Tests pass (`pnpm run test`)

## Progress

- **T46a COMPLETED** (2026-08-27): `TurnLifecycle` owns the complete turn
  lifecycle and the hook is a 217-line adapter. The send path delegates to
  `ChatTurnCoordinator`; 43 test files / 362 tests pass.
- Remaining phases depend on T60a registry work and model-history policy
  consolidation (T48b/T48c/T62a/T64b).

## Related

- `refactored-architecture.md` — Canonical architecture reference
- `agentic-tool-calling.md` — Tool calling design patterns (still valid)
- T22 — ChatApp.tsx decomposition (completed)
- T23 — Settings.ts decomposition (completed)
- T60a–T60c — canonical registry, transport parity, and execution boundary
- T46a — chat turn coordinator and request lifecycle decomposition
- T48b/T48c/T62a — model-history policy, compaction, and elision regression

## Audit Update — 2026-08-25

Handler extraction alone is insufficient. Prompt descriptions, schemas,
dispatch, previews, formatting, risk, and availability must converge on the
canonical registry so this task does not split one overloaded class while retaining
several independent sources of truth.

## Architecture Review Update — 2026-08-27

The recommended implementation order is:

1. Complete the remaining T60a registry ownership and projection work.
2. Physically decompose `ToolExecutor.ts` around the T60 registry and pipeline.
3. Consolidate model-history policy through T48b/T48c/T62a and the T64b
   retention evidence.
4. Execute T46a to move request assembly and turn lifecycle out of
   `useMessageActions.ts`.
5. Reassess `api.ts` and `main.ts` as separate later phases.

Do not split by arbitrary function groups. Each extracted module must own a
complete concept and leave the original file as a thin composition layer.

## Implementation Progress — 2026-08-27

The reconciled implementation has been checked locally:

- `ToolExecutor.ts` is a 292-line compatibility layer. It builds one resolved
  registry, validates calls, and delegates to eight domain handler modules.
- `ChatTurnCoordinator.ts` selects native or OpenResponses execution from one
  React-free entry point; `ChatTurnOutput.ts` owns tool-output collection.
- `TurnLifecycle` remains the outer owner of request lifecycle actions and now
  delegates its tool-enabled send path to `runChatTurn()`.
- `useMessageActions.ts` is 217 lines; `api.ts` is 363 lines; and `main.ts` is
  226 lines. The latter two use the main-branch provider/history/streaming and
  registration/storage/event modules.
- Prompt definitions, model tool exposure, and execution all use the resolved
  registry from the same `ToolExecutor` instance.
- The local suite passes 43 test files / 362 tests, TypeScript passes, and the
  production build passes. Provider-switching and real-provider acceptance
  remain separate runtime gates.
The first commit is `5dda5df`; the capability-domain split is the next local
commit. The branch is based on `a15c47646e1141239b269c8f608331889b1b32df`.

## Verification Update — 2026-08-29

The user confirmed that T46 testing passes. This session also ran the full
local suite: 43 test files and 368 tests passed, and TypeScript compilation
passed. Provider switching and real-provider acceptance remain separate
runtime checks, so the task stays active until those checks are recorded.

The temporary handler grouping has now been replaced with separate note, bulk,
discovery, vault, web, memory, session, and settings modules. Each uses the
same shared host-service context and continuation store.

T46a has also moved prompt creation, history projection, request budgeting, and
model-message assembly into `ChatTurnRequest.ts`. The hook remains above its
target size because turn callbacks, approval state, and persistence are still
inside it.

Completed assistant-message creation and session updates have now moved into
`ChatTurnPersistence.ts`. The remaining hook work is UI callback, interruption,
and approval lifecycle code.

Turn output collection has now moved into `ChatTurnOutput.ts`. It owns the
temporary text, tool-call, tool-result, and content-part state for one turn
without depending on React or Obsidian. The hook remains responsible for
visible streaming updates, interruption display, and approval prompts. The
hook is now 1,252 lines. The full suite passes 43 test files and 362 tests;
TypeScript and the production build also pass.

## Review and Merge Closeout — 2026-08-28

PR #7 was reviewed, corrected, and merged into `main` as `975bb7e`.

- Agent-provider `/create`, `/edit`, and `/append` slash commands now route
  through `runChatTurn()` and the OpenResponses path.
- Manual tool approval reuses the active `ToolExecutor`, preserving
  continuation and pagination state across the approval boundary.
- `turnLifecycle.ts` was formatted and focused regression tests were added.
- The merged tree passes 42 test files / 363 tests, TypeScript, the production
  build, the changed-file Prettier check, and `git diff --check`.
- T46 remains active because provider-switching and real-provider acceptance
  still require runtime verification.

## Fresh Architecture Review — 2026-08-29

The fresh read-only `improve-codebase-architecture` review examined source at
`63bce58` after the T46/T46a reorganization. The reports are retained in
`memory-bank/architecture-reviews/`, with the dated comparison in
`implementation-details/T46-architecture-review-2026-08-29.md`.

The original tool-registry and hook-level send/request findings are addressed.
The top remaining finding is to consolidate model-history policy behind one
model-ready history boundary owned by T48/T48a/T48b/T48c/T62a. `TurnLifecycle`
is a reassessment boundary after that policy seam, not an automatic new split.
Capability-construction cleanup remains with T60/T60a. At the time of this
review, sync decomposition was deferred; it was later completed under T67.
T46 remains active for provider switching and real-provider runtime
acceptance.

## Implementation Update — 2026-08-29

Added `src/context/modelHistory.ts` as the shared model-history entry point.
`TurnLifecycle` now uses it for replay, pairing checks, and request budgeting;
the agent loops use its shared continuation budget and result-limit helpers.
Agent turns automatically use preserve mode, while ordinary chat keeps the
configured setting. The saved transcript is unchanged.

Verification passed: 45 test files / 381 tests, TypeScript, formatting, and
the production bundle. Provider switching and real-provider runtime acceptance
remain open.

## 2026-08-30 Turn Action Update

The stop, retry, edit, cancel-edit, and tool-approval actions were moved into
`src/agent/turnActions.ts` in commit `fabc4ef`. `turnLifecycle.ts` remains the
owner of the main send path and execution policy. The new file is 169 lines;
`turnLifecycle.ts` is 1,019 lines.

Storage lifecycle work was completed separately under T67. T46 does not take
ownership of that code. Its remaining work is provider switching and
real-provider runtime acceptance, followed by the planned model-history
reassessment.
