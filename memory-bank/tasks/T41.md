# T41: Plugin Auto-Updater with Stable/Dev Channels

*Created: 2026-08-09*
*Last Updated: 2026-08-14 16:42:00 IST*

## Task Information
**Title:** Plugin Auto-Updater with Stable/Dev Channels
**Status:** ✅ COMPLETE — Commit-hash fix applied and tested
**Priority:** HIGH
**Created:** 2026-08-09
**Started:** 2026-08-09
**Completed:** 2026-08-09

## Description

Build a complete custom auto-updater for the obsidian-ai plugin that can check for updates from GitHub releases, download and install them, and rollback on failure. Support both stable and dev (pre-release) channels with automatic or manual update modes.

## Acceptance Criteria
- [x] Check GitHub releases for updates via API
- [x] Support stable vs dev (pre-release) channels
- [x] Auto-install stable updates silently (optional toggle)
- [x] Manual check with "Check Now" button in settings
- [x] Backup current plugin before install, rollback on failure
- [x] Cross-platform: works on desktop AND mobile
- [x] Semver comparison for version detection
- [x] Non-semver dev tags handled (e.g., `latest-dev`)
- [x] Commit-hash comparison to prevent perpetual "update available" on dev channel
- [x] Test auto-updater end-to-end with actual release

## Implementation Details

### Architecture
- `src/updater/PluginUpdater.ts` — Core updater class
  - Fetches releases from GitHub API
  - Compares versions using semver
  - Downloads release asset
  - Backs up current installation
  - Extracts and installs new version
  - Rolls back on failure
- `src/settings-sections/updaterSettings.ts` — Settings UI
  - Channel selector (stable/dev)
  - Auto-install toggle
  - Manual "Check Now" button
  - Version display with channel suffix
  - Update metadata modal showing commit hash, message, author, and timestamp

### Key Design Decisions
- **Cross-platform:** Uses `requestUrl` + `app.vault.adapter` instead of Node.js `fs`/`https`
- **Mobile-safe:** No Node.js imports; pure Obsidian APIs
- **Backup strategy:** Copies current `main.js` + `manifest.json` + `styles.css` before install
- **Rollback:** Restores backup files if new version fails to load

### Fixes Applied During Development
| Issue | Fix |
|-------|-----|
| Settings nav links broken for "Sync" / "Updates" | Fixed section IDs to match nav generation |
| Mobile UI completely garbled | Removed Node.js imports; rewrote with Obsidian cross-platform APIs |
| Mobile toggles showed as rectangles | Switched to `Setting.addToggle()` / `Setting.addDropdown()` |
| Dev channel checked stable releases | Now finds `prerelease: true` specifically |
| Version display lacked channel info | Added `(stable)` / `(dev channel)` suffix |
| Non-semver dev tags broke comparison | `latest-dev` now treated as newer than semver |

## Related Files
- `src/updater/PluginUpdater.ts`: Core updater logic
- `src/settings-sections/updaterSettings.ts`: Settings UI
- `src/main.ts`: Plugin lifecycle integration
- `src/settings.ts`: Settings schema additions
- `styles.css`: Updater-specific styles

## Dependencies
- **Depends On:** T7 (Release System & CI/CD)
- **Blocks:** —

## Progress Tracking
- **2026-08-09**: Core updater implemented with stable/dev channels
- **2026-08-09**: Cross-platform fixes for mobile compatibility
- **2026-08-09**: Settings nav and UI fixes
- **2026-08-09**: Commit-hash fix applied — dev channel now compares `GIT_COMMIT_HASH` with GitHub's main branch HEAD
- **2026-08-09**: `latest-dev` release rebuilt from ae09179 with fix included
- **2026-08-09**: Verified release contains correct commit hash (ae09179)
- **2026-08-14**: Update modal now displays GitHub-linked commit hash, commit message, author, and timestamp; export icon styling aligned with neutral action icons (`7a770f3`).

## Issues and Blockers
- **Dev channel perpetual "update available"**
  - Root cause: `latest-dev` is non-semver, so `compareVersions("latest-dev", "1.2.5")` always returns > 0
  - **Fix applied:** `checkForUpdate()` now accepts `currentCommitHash` parameter. For dev channel, fetches latest commit SHA from GitHub API (`repos/{repo}/commits/main`), compares with local `GIT_COMMIT_HASH`. If match, returns `hasUpdate: false` with `commitMatch: true`.
  - Files modified: `src/updater/PluginUpdater.ts`, `src/main.ts`
  - **Status:** ✅ Fixed and verified — `latest-dev` release rebuilt from ae09179

## Notes
- The auto-updater is disabled on mobile by default (can be enabled)
- GitHub API rate limiting may affect update checks (60 req/hr unauthenticated)
- BRAT distribution is separate from auto-updater — BRAT is for beta testing, auto-updater is for end users

## Related Resources
- GitHub releases page for obsidian-ai plugin
- Obsidian API docs: `requestUrl`, `Vault.adapter`
