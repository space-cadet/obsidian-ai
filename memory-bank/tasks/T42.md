# T42: Remote Chat Storage & Sync

*Created: 2026-08-10*
*Status: ✅ MERGED TO MAIN*
*Last Updated: 2026-08-19*
*Branch: `t42-remote-storage` → merged via PR #3*
*Merge Commit: `d555c792`

## Objective

Enable persistent, cross-device synchronization of chat sessions via remote storage backends. Users should be able to access their chat history from any device without relying on local-only storage.

## Motivation

Currently, chat sessions are stored locally in Obsidian's plugin data. This means:
- **No cross-device continuity**: Chats on desktop don't appear on mobile
- **Data loss risk**: Local storage can be corrupted or lost
- **No backup**: No way to recover chat history
- **Multi-device friction**: Users must manually manage sessions per device

## Scope

### In Scope
- Pluggable storage backend architecture (S3-compatible, WebDAV, custom server)
- End-to-end encryption of chat data at rest
- Incremental sync (only changed sessions)
- Conflict resolution for concurrent edits
- Offline queue with sync-on-reconnect

### Out of Scope (for v1)
- Real-time collaborative editing (covered by T40 relay)
- Large file/attachment sync (T19 handles local attachments)
- Automatic cloud provider provisioning

## Architecture

### Design Doc
`memory-bank/implementation-details/remote-chat-storage.md`

### High-Level Flow

```
[Obsidian Desktop] ──sync──► [Remote Storage] ◄──sync── [Obsidian Mobile]
      │                            │
      └─ local cache ──────────────┘
```

### Key Components

1. **StorageAdapter Interface** - Pluggable backends
2. **SyncEngine** - Delta sync, conflict resolution, encryption
3. **LocalCache** - IndexedDB / localStorage for offline support
4. **EncryptionLayer** - AES-256-GCM with user-provided passphrase

## Implementation Phases

### Phase 1: Architecture & Interface ✅ COMPLETE
- [x] Define `StorageAdapter` interface (`src/sync/StorageAdapter.ts`)
- [x] Implement `LocalCache` with IndexedDB (`src/sync/LocalCache.ts`)
- [x] Implement `EncryptionLayer` — AES-256-GCM, PBKDF2, zero-knowledge (`src/sync/EncryptionLayer.ts`)
- [x] Implement `SyncEngine` — delta sync, conflict resolution (3 strategies), state machine (`src/sync/SyncEngine.ts`)
- [x] TypeScript clean, 236/236 tests pass

### Phase 2: WebDAV Backend ✅ COMPLETE
- [x] Implement `WebDAVStorageAdapter` (Nextcloud, ownCloud, generic WebDAV)
- [x] Settings integration: RemoteStorageConfig types + defaults
- [x] Settings UI: backend selector, credentials, test connection, manual sync
- [x] Wired into SettingsTab navigation and render pipeline
- [x] Committed and pushed (`ac24ced` → `b9a4c949` → `31d9158` → `7ab9614` → `e96b703`)

### Phase 2 Fixes Applied
- [x] Missing import — added `renderRemoteStorageSection` import
- [x] Checkbox rendering — replaced raw `<input>` with Obsidian `ToggleComponent`
- [x] childNodes API — rewrote as vanilla `createEl` calls
- [x] Web Crypto types — cast `Uint8Array` to `BufferSource`
- [x] Null safety — added non-null option for `CryptoKey`
- [x] Missing salt — added to `EncryptSession` payload
- [x] Missing size — added `size?: number` to `SyncSessionMeta`
- [x] 'Fetch' failed — switched from `fetch()` to `requestUrl()` for Electron sandbox compatibility
- [x] Passphrase required — made optional with 'Encrypt Data' toggle
- [x] **ETag comparison** — replaced timestamp comparison with ETag to stop false re-downloads (`be3c3bb`)
- [x] **Terminal-style progress modal** — progress bar, per-session log, elapsed time, session titles (`29ad150`)
- [x] **Sync log files** — local (`sync.log`) + remote (`sync.log`) recording every operation (`29ad150`)
- [x] **Cancel stops sync** — `_cancelled` flag checked between sessions, finishes current then stops (`deff496`)

### What's Working
- Settings UI renders correctly
- WebDAV config form with test connection button
- Optional encryption (plaintext mode for testing)
- Settings persist to plugin data
- **SyncEngine fully wired** — lazy-init on first sync, cache populated from chat data
- **WebDAV directory auto-creation** — prefix + sessions/ created on first sync
- **Static `requestUrl` import** — works in bundled plugin context
- **Secure password storage** — WebDAV password saved to Obsidian localStorage, not data.json
- **Sync progress modal** — terminal-style log with progress bar, per-session status, elapsed time
- **Toast notifications** — success/error notices after sync completes
- **Auto-updater branch support** — fetches commit from current branch, not main
- **ETag comparison** — no more false re-downloads due to clock skew
- **Sync log files** — every operation recorded locally and remotely
- **Cancel works** — stops sync between sessions without half-written files

### Phase 3: S3-Compatible Backend 🔄 NEXT
- [ ] Implement `S3StorageAdapter` (AWS S3, MinIO, Backblaze B2, Wasabi)
- [ ] Support bucket-per-user or prefix-per-user
- [ ] Implement delta sync using ETags / checksums
- [ ] Add progress callbacks for large syncs
- [ ] Handle WebDAV-specific quirks (locks, properties)

### Phase 4: Custom Server Backend
- [ ] Implement `CustomServerAdapter` (REST API)
- [ ] Support authentication (API key, JWT)
- [ ] Companion server implementation (optional, Node.js)

### Phase 5: Polish
- [ ] Conflict resolution UI (merge, overwrite, keep both)
- [ ] Sync status indicator in chat UI
- [ ] Manual sync trigger + auto-sync intervals
- [ ] Import/export for migration

### Phase 6: Port SyncIt Features 🔄 NEW (2026-08-19)
Port proven sync features from SyncIt to make obsidian-ai sync faster, safer, and more reliable.

| Subtask | Feature | Source | Priority | Status |
|---------|---------|--------|----------|--------|
| [T42a](tasks/T42a.md) | Sync Index (skip unchanged) | SyncIt `SyncIndexManager` | P1 | 🔄 |
| [T42b](tasks/T42b.md) | Atomic Writes | SyncIt `AtomicWrite.ts` | P1 | 🔄 |
| [T42c](tasks/T42c.md) | Concurrency Control | SyncIt `runWithConcurrency()` | P1 | 🔄 |
| [T42d](tasks/T42d.md) | Server Signature | SyncIt `makeServerSignature()` | P1 | 🔄 |
| [T42e](tasks/T42e.md) | Dry Run Mode | SyncIt `performDryRun()` | P2 | 🔄 |
| [T42f](tasks/T42f.md) | Progress UI (sidebar) | SyncIt `SyncSidebarView` | P2 | ⛔ SUPERSEDED by T43 |
| [T42g](tasks/T42g.md) | — (see T43) | — | — | See T43 for integrated UI |

**Phase 6b (T43): Integrated Sync UI** 🔄 NEW (2026-08-20)

Replaces the standalone sync sidebar with an integrated sync tab inside Chat Lab. See [T43](tasks/T43.md) for full details.

| Subtask | Feature | Status |
|---------|---------|--------|
| T43 | Integrate Sync UI into Chat Lab | 🔄 In Progress |

**T43 removes the separate `SYNC_SIDEBAR_VIEW_TYPE` and moves sync into the chat tab system. Export button becomes a dropdown with Export + Sync options. Sync opens as a non-session tab with rich progress UI.**

## Design docs:
- `implementation-details/sync-index-design.md`
- `implementation-details/atomic-writes-design.md`
- `implementation-details/concurrency-control-design.md`
- `implementation-details/server-signature-design.md`
- `implementation-details/dry-run-design.md`
- `implementation-details/progress-ui-design.md`

## Security Model

### Encryption
- **At rest**: AES-256-GCM, key derived from user passphrase
- **In transit**: HTTPS/TLS (handled by backend)
- **Key management**: Passphrase never stored, only derived key in memory

### Privacy
- Server never sees plaintext (zero-knowledge)
- Metadata (session count, timestamps) may be visible depending on backend
- User controls backend selection and credentials

## Configuration

```typescript
interface RemoteStorageConfig {
    enabled: boolean;
    backend: "s3" | "webdav" | "custom";
    encryption: {
        enabled: boolean;
        passphrase?: string; // Prompted if not set
    };
    syncInterval: number; // minutes, 0 = manual only
    s3?: {
        endpoint: string;
        bucket: string;
        region: string;
        accessKeyId: string;
        secretAccessKey: string;
    };
    webdav?: {
        url: string;
        username: string;
        password: string;
    };
    custom?: {
        url: string;
        apiKey: string;
    };
}
```

## Open Questions

1. **Conflict resolution strategy**: Last-write-wins vs. CRDT vs. manual merge?
2. **Attachment handling**: Sync attachments or store references only?
3. **Retention policy**: Auto-delete old sessions from remote?
4. **Multi-vault sync**: Same remote backend, different vaults?

## Related Tasks

- T40: Multi-User Chat (real-time sync via relay)
- T19: File Attachments (large file handling)
- T15: Tabbed Chat (session management)
- T42a–T42f: SyncIt feature ports (Phase 6)

## Files

| File | Action | Purpose |
|------|--------|---------|
| `src/sync/StorageAdapter.ts` | Create | Interface definition |
| `src/sync/S3StorageAdapter.ts` | Create | S3 implementation |
| `src/sync/WebDAVStorageAdapter.ts` | Create | WebDAV implementation |
| `src/sync/CustomServerAdapter.ts` | Create | Custom REST API |
| `src/sync/SyncEngine.ts` | Create | Core sync logic |
| `src/sync/EncryptionLayer.ts` | Create | AES-256-GCM wrapper |
| `src/sync/LocalCache.ts` | Create | IndexedDB cache |
| `src/components/RemoteStorageSettings.tsx` | Create | Settings UI |
| `src/components/SyncStatusIndicator.tsx` | Create | Status badge in chat |

## Recent Commits (t42-remote-storage branch)

| Commit | Description |
|--------|-------------|
| `deff496` | Cancel button actually stops sync (cancellation flag between sessions) |
| `29ad150` | Terminal-style progress modal, sync log files (local + remote), ETag comparison |
| `be3c3bb` | ETag comparison instead of timestamps to detect remote changes |
| `820fdf4` | Secure password storage, sync progress modal, toast notifications, fix updater branch commit hash |
| `19e5a31` | Static import for requestUrl (fix dynamic import failure) |
| `543260f` | Auto-create prefix directory, recursive mkcol for nested paths |
| `1fc3162` | Lazy-init sync engine, populate cache from chat data, handle 404 in listSessions |
| `9cd29cd` | Wire SyncEngine into plugin lifecycle, feature branch CI, auto-updater branch awareness |
