# T15: Tabbed Chat Interface with Multi-Profile

*Created: 2026-05-15*
*Last Updated: 2026-08-05 10:55:22 IST*

**Status**: 🔄 **IN_PROGRESS**
**Priority**: high

## Details

[Details](tasks/T15.md)

Fix 4 critical streaming/chat UI bugs: Android flicker, interrupted message loss, retry attachment loss, live token counting

Fix token counter accumulation, remove green streaming border, add live tool result updates without re-rendering entire bubble

## 2026-07-29: Past-Session Search and Shared Tabs

- Repaired saved-session indexing across JSONL and legacy storage.
- Made `search_past_sessions` explicit in agent prompts and excluded the current conversation from results.
- Added titled inline result links that open and scroll to the matched message.
- Replaced stacked sidebar leaves with shared internal tabs, a shared toolbar/composer, tab closing, and horizontal scrolling.
- Confirmed Shift+Enter and Cmd/Ctrl+Enter send messages when Enter is configured for new lines.

Implementation details: [Past-Session Search and Shared Tabs](../implementation-details/past-session-search-and-tabs.md).

**Deferred:** Further tab-heading visual polish, at user request.

**Attribution:** All 2026-07-29 implementation work was performed by **GPT 5.6 Terra Low**.

## 2026-08-05: Settings Navigation and Draft Tab Lifecycle

- Repaired Settings section navigation, including the `Intelligence` shortcut's `AI Intelligence Layer` target.
- Added the persisted 120–360 px tab-title width setting, defaulting to 160 px, and compacted the diagnostics metric layout.
- Defined and implemented draft tabs: every `+` opens a distinct draft tab; a draft enters saved history only after its first message; closing an unsent draft discards it.
- Added history-view and persistence-boundary guards that exclude drafts and clean up legacy zero-message entries.

Implementation details: [Past-Session Search and Shared Tabs](../implementation-details/past-session-search-and-tabs.md).

**Known follow-up:** Successive unsent tabs currently use the fallback `Chat <current date>` title after the first `New chat` tab. This is cosmetic only; a later refinement can keep all unsent tabs visibly titled `New chat`.

## 2026-08-05: Per-Tab Runtime Isolation Follow-Up

- Confirmed a crucial runtime isolation bug: internal tabs share one live streaming/tool process state even though saved messages are session-scoped.
- Created T34 as the corrective subtask for tab-scoped generation state, stop behavior, pending tool approvals, token totals, and tool-session identity.
- Planned a session-keyed runtime map so the toolbar/composer can remain shared while active generation state is owned by the originating `sessionId`.

Implementation details: [Per-Tab Chat Process Isolation](../implementation-details/per-tab-chat-process-isolation.md).

## Dependencies

- T13
- T14
- T9
- T34
