# T43: Multi-User and Agent Chat with LaTeX Support

*Created: 2026-08-10*
*Updated: 2026-08-12 10:35:04 IST*
*Depends on: T40 (Multi-User Chat with LaTeX Support)*
*Status: ✅ COMPLETE — All 5 Phases Delivered and Merged*

## Objective

Evolve the multi-user chat system (T40) to support **equal-footing participants** — both AI agents and remote human users participate in the same chat tab, with messages routed to all participants and each deciding independently how to respond.

**Branch:** `t43-multi-user-agent-chat` (branched from `main` at commit `19f780d`)
**Delivery:** All phases merged into `main` on 2026-08-11. Final commits: `90503d9` (Phase 5), `ab23e5f` (post-phase fixes)

## Background

T40 built the WebSocket relay infrastructure (PoP) and presence tracking (Phase 2). The current architecture treats remote users as "connections" rather than first-class participants. Messages sent to the relay are also sent to the local AI, but there's no clean model for:
- Selecting which remote users participate in which tab
- Routing messages to all participants (agents + humans) equally
- Including remote user messages in agent context
- Creating human-only tabs (no AI agents)

This task addresses those gaps.

## Architecture: Equal-Footing Participant Model

### Core Principle
All participants — AI agents, remote humans, local user — are treated equally in a chat tab.

### Participants

| Type | Source | How Added | Role |
|------|--------|-----------|------|
| AI Agent | Local agent registry | Agent dropdown (existing) | Receives messages, may respond |
| Remote User | Relay user list | User dropdown (new) | Receives messages via relay |
| Local User | You | Always present | Sends messages, sees all |

### Message Flow

```
User A (Client X) sends message in Tab T1
    │
    ├──► AI Agent A1 (in T1) ──► Receives message + context ──► May respond
    │
    ├──► Relay Server ──► User B (Client Y, Tab T2) ──► Displayed
    │
    └──► Local display (User A sees their own message)

User B (Client Y) sends message in Tab T2
    │
    ├──► AI Agent A1 (in T1) ──► Receives message + context ──► May respond
    │       (remote messages included in context!)
    │
    ├──► Relay Server ──► User A (Client X, Tab T1) ──► Displayed
    │
    └──► Local display (User B sees their own message)
```

### Key Rules

1. **Broadcast to all**: Every message sent in a tab is delivered to ALL participants in that tab
2. **Agents see everything**: AI context includes messages from ALL participants (local + remote)
3. **No AI without agents**: If a tab has NO AI agents, messages go to relay only (no AI trigger)
4. **Remote users are participants**: Not just connections — they have presence, can be selected per-tab

## Implementation Strategy: Two-Step Approach

**Step 1: ParticipantRouter Wrapper** (current focus)
- Create a `ParticipantRouter` class that wraps the existing `Orchestrator`
- Keeps all agent logic untouched — minimal risk
- Adds relay routing for remote users alongside agent dispatch
- Validates the participant model before deeper refactor
- If it breaks: disable wrapper, fall back to old group chat behavior

**Step 2: Refactor Orchestrator** (only if Step 1 validates the model)
- Make `Orchestrator` participant-agnostic directly
- Remove wrapper indirection
- Only if needed for features like remote users in debates or agent-to-remote routing

## Implementation Phases

### Phase 1: Foundation ✅ COMPLETE (2026-08-11)
- [x] Extend `ChatMessage` type with `remote?: boolean` and `fromUserId?: string`
- [x] Extend `ChatSession` type with `remoteUsers?: string[]`
- [x] Update `WebSocketSyncAdapter` to set `remote: true` on received messages
- [x] Create `ParticipantRouter` skeleton (wraps Orchestrator, adds relay dispatch)
- [x] Initialize `remoteUsers: []` in all session creation paths
- **Commit:** `539ca52`
- **Tests:** All 188 tests pass

### Phase 2: Wire Router into ChatApp ✅ COMPLETE (2026-08-11)
- [x] Replace `handleSendWithSync` with `ParticipantRouter.dispatch`
- [x] Build participant list from `selectedProfileIds` + `remoteUsers`
- [x] Test: agent receives message, remote user receives relay
- [x] Test: message appears in local chat
- **Commit:** `f83e5d0`
- **Tests:** All 188 tests pass

### Phase 3: AI Context with Remote Messages ✅ COMPLETE (2026-08-11)
- [x] Include remote messages in AI context with attribution (`[Remote User <id>]: message`)
- [x] Add participant list to agent prompt context (`[Participants]` section)
- [x] Distinguish remote user messages in agent context
- [x] Update `buildSystemPrompt` with optional `participants` parameter
- [x] Update `Orchestrator` with `remoteUsers` field and context attribution
- [x] Update `ParticipantRouter` to sync remote users with Orchestrator
- [x] Update `useMessageActions` single-chat history with remote attribution
- **Commit:** `9b2a498`
- **Tests:** All 188 tests pass

### Phase 4: Human-Only Tabs ✅ COMPLETE (2026-08-11)
- [x] Allow selecting remote participants with zero AI agents
- [x] Skip AI trigger when no agents are present
- [x] Pure relay mode sends messages through `ParticipantRouter` without an `Orchestrator`
- **Tests:** Relay-only `ParticipantRouter` coverage added; full suite passes

### Phase 5: Attribution & UI ✅ COMPLETE (2026-08-11)
- [x] **Phase 5a: Message Attribution**
  - Fixed `MessageBubble.tsx` to show `fromUserId` (e.g., "samsung-tab") instead of "You" for remote messages
  - Remote messages now display sender name with colored dot
- [x] **Phase 5b: Typing Indicators**
  - Extended `SyncAdapter` interface with `sendTyping()` and `onTyping()` methods
  - Implemented in `WebSocketSyncAdapter.ts` — sends `{type: "typing", sender: userId}` via relay
  - Added `typingUsers` state in `ChatApp.tsx` with 3-second auto-clear
  - Added throttled typing emitter in `ChatInput.tsx` (2-second cooldown)
  - Added typing indicator UI in `ChatMessages.tsx` — shows "X is typing…" with animated dots
  - Added CSS: `.chat-typing-indicator-remote`
- [x] **Phase 5c: Participant List Bar**
  - Added persistent participant bar below ActionBar showing:
    - Selected AI agents with colored dots
    - Remote/connected users with green dots
  - Added CSS: `.chat-participant-bar`, `.chat-participant-chip`, `.chat-participant-dot-online`
- [x] **Relay Bug Fix**
  - Fixed `relay/server.js` — `broadcast()` was receiving `Buffer` objects instead of strings
  - Changed `broadcast(roomId, ws, raw)` → `broadcast(roomId, ws, raw.toString())` in two places
  - Messages now flow correctly between mobile devices
- [x] **Post-Phase 5 Bug Fixes**
  - Fixed ActionBar badge showing "1" instead of "0" when no agents selected (changed `&&` to `?? 0`)
  - Fixed participant bar not showing single selected agents (added `selectedAgents` array separate from `participants`)
- **Commits:** `90503d9` (Phase 5), `ab23e5f` (fixes)
- **Tests:** Relay tested MacBook ↔ mobile; participant bar verified; typing indicators need 2-device test

### Follow-up: Mobile Chat Hardening and Selection Count ✅ COMPLETE (2026-08-12)
- [x] Fixed mobile transcript scrolling by constraining the flex chain with `min-height: 0`.
- [x] Added touch-oriented scrolling behavior and prevented scroll chaining in the message area.
- [x] Removed mobile composer bottom padding below the attachment toolbar; the remaining view-selector gap is Obsidian host chrome.
- [x] Fixed the model-selection badge to count `selectedProfileIds` directly; remote-user counts remain separate.
- [x] Added ActionBar regression tests for zero, one, and two selected models and separate remote-user counts.
- [x] Formatted all touched files so the changed-files-only Prettier workflow passes.
- **Verification:** 21 test files, 206 tests passed; Prettier check and `git diff --check` passed.
- **Known environment issue:** TypeScript checking remains blocked by the existing AI SDK API mismatch in `src/api.ts` (`isStepCount`, `LanguageModelV4`, and `StreamTextResult.stream`).

## Delivery and Verification

- PR #1: `https://github.com/space-cadet/obsidian-ai/pull/1` (Phases 1-4)
- Final implementation commits: `539ca52` through `ab23e5f`
- Full test suite: 21 files, 206 tests passed
- Changed-files-only Prettier check: passed
- `git diff --check`: passed
- **Branch merged:** `t43-multi-user-agent-chat` → `main` (fast-forward), pushed to GitHub

## Files

| File | Action | Purpose |
|------|--------|---------|
| `src/types.ts` | Modify | Add `remote`, `fromUserId` to `ChatMessage` |
| `src/hooks/useChatSession.ts` | Modify | Track `remoteParticipants` in session state |
| `src/hooks/useMessageActions.ts` | Modify | Route to all participants, include remote in context |
| `src/components/ChatApp.tsx` | Modify | Wire participant selection, handleSend routing, typing state |
| `src/components/ActionBar.tsx` | Modify | Add user selection dropdown, badge fix |
| `src/components/__tests__/ActionBar.test.tsx` | Create | Regression coverage for model and remote-user badges |
| `src/components/ChatInput.tsx` | Modify | Throttled typing emitter |
| `src/components/ChatMessages.tsx` | Modify | Typing indicator UI |
| `src/components/MessageBubble.tsx` | Modify | Remote user attribution styling |
| `src/sync/SyncAdapter.ts` | Modify | `sendTyping()`/`onTyping()` interface |
| `src/sync/WebSocketSyncAdapter.ts` | Modify | Set `remote: true`, typing implementation |
| `styles.css` | Modify | Participant bar, typing indicator, remote message styles |
| `relay/server.js` | Modify | Buffer→string fix in broadcast |

## Related Tasks

- T40: Multi-User Chat with LaTeX Support (relay infrastructure, presence)
- T42: Remote Chat Storage & Sync (complementary — future: persist cross-device sessions)

## Design Docs

- `memory-bank/implementation-details/multi-user-agent-chat.md` (this architecture)
- `memory-bank/implementation-details/presence-tracking.md` (T40 Phase 2 presence)
- `memory-bank/implementation-details/multi-user-chat-design.md` (T40 original design)

---

*T43 is complete. All 5 phases delivered. The equal-footing participant model is fully functional.*
