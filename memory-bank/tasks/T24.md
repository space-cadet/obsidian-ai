# T24: SessionStorage — JSONL Chat Persistence

## Task Metadata
- **ID:** T24
- **Title:** SessionStorage — JSONL Chat Persistence
- **Status:** ✅ COMPLETED
- **Priority:** HIGH
- **Started:** 2026-06-14
- **Completed:** 2026-06-14

## Description
Implement persistent chat session storage using JSONL (JSON Lines) format for human-readable, append-only, git-friendly logs. Replace or supplement in-memory state with disk-backed sessions that survive Obsidian restarts.

**2026-06-15: Search feature added on top of SessionStorage** — fuzzy search across all session JSONL files with UI integration.

## Implementation

### Phase 1: SessionStorage Core (0mu-a)
- **File:** `src/storage/session-storage.ts`
- **Class:** `SessionStorage`
- **Methods:**
  - `loadSession(id: string): ChatMessage[]` — reads `sessions/{id}.jsonl`, parses each line as JSON
  - `appendMessage(id: string, message: ChatMessage): void` — appends one JSONL line
  - `createSession(): { id: string, file: TFile }` — creates new `sessions/{uuid}.jsonl`, returns handle
- **Storage location:** `.obsidian/plugins/obsidian-ai/sessions/{uuid}.jsonl`

### Phase 2: ChatStorage Wrapper (0mu-b)
- **File:** `src/storage/ChatStorage.ts`
- **Class:** `ChatStorage` — async wrapper around SessionStorage
- **Methods:**
  - `saveSession(id, messages): Promise<void>`
  - `loadSession(id): Promise<ChatMessage[]>`
  - `listSessions(): Promise<SessionMetadata[]>`
  - `deleteSession(id): Promise<void>`
- **Integration:** Used by ChatApp for loading/saving conversation history

### Phase 3: Plugin Integration (0mu-c)
- **File:** `src/main.ts`
- **Changes:**
  - Added `sessionStorage: SessionStorage` property to `ObsidianAIPlugin`
  - Initialized in `onload()` with `app`, `manifest`, `logger` dependencies
  - Wired `ChatApp` to use `ChatStorage` for session persistence
- **Fix:** Added missing `contextPickerPathDisplay` to `ObsidianAISettings` interface (was breaking build)
- **Build:** `npm run build` passes ✅

### Migration Support
- **File:** `src/storage/Migration.ts`
- Handles migration from old in-memory format to JSONL
- **File:** `src/modals/MigrationPromptModal.ts`
- UI modal prompting user to migrate existing sessions

## Files Changed
- `src/storage/session-storage.ts` ⭐ NEW
- `src/storage/ChatStorage.ts` ⭐ NEW
- `src/storage/Migration.ts` ⭐ NEW
- `src/modals/MigrationPromptModal.ts` ⭐ NEW
- `src/main.ts` — integration + settings fix
- `src/components/ChatApp.tsx` — storage integration
- `src/context/tokenEstimator.ts` — updates for storage-aware counting
- `src/hooks/useMessageActions.ts` — updates for storage integration
- `src/settings.ts` — settings interface fix
- `src/settings-sections/advanced.ts` — advanced settings updates
- `src/agent/AgentLoop.ts` — agent loop integration
- `src/agent/OpenResponsesLoop.ts` — open responses integration

## Commit
`fdc8b58` — feat: SessionStorage with JSONL persistence + plugin integration

---

## Search Feature (Added 2026-06-15)

### Phase 4: Search Index (87z-a)
- **File:** `src/search/index.ts`
- **Class:** `SearchIndex` — in-memory inverted index from session JSONL files
- Features: stop-word filtering (70 words), posting list intersection, 200-char snippet extraction, recency sorting

### Phase 5: Search Input UI (87z-b)
- **File:** `src/components/SearchInput.tsx`
- Debounced input (300ms), clear button (×), "Search chats…" placeholder
- CSS matching Obsidian theme variables

### Phase 6: Fuzzy Search + Results Display (87z-c)
- **File:** `src/search/fuzzy-search.ts` — `FuzzySearcher` class, score threshold 0.4, highlighted match ranges
- **File:** `src/components/search-results.tsx` — results list with title badge, highlighted snippets, empty/no-results states, click-to-open-session
- Manually wired into `ChatApp.tsx` with `FuzzySearcher` ref, search state, `useEffect` for query handling

### Phase 7: UI Polish
- **CSS fix:** Garbled search results display (overlap/overflow) — title ellipsis, snippet max-height, flex containment
- **Click behavior:** Open full session + scroll to specific message + 2-second highlight animation (`chat-message-highlight` CSS)
- **Toggle visibility:** Search button in ActionBar toolbar (magnifying glass icon), hidden by default, active state styling
- **Conditional render:** Search results only show when query is non-empty (prevents chat area compression)

### Search Commits
- `ef89888` — Wired search feature (manual ChatApp integration)
- `6b90eb7` — CSS fix: search results overlap
- `d60c5cd` — Search results open full session + scroll to message
- `f620472` — Search results only show when query exists
- `503d8a7` — Search toggle button in toolbar

### Search Files Changed
- `src/search/index.ts` ⭐ NEW
- `src/search/fuzzy-search.ts` ⭐ NEW
- `src/components/SearchInput.tsx` ⭐ NEW
- `src/components/search-results.tsx` ⭐ NEW
- `src/components/ChatMessages.tsx` — wrapped MessageBubble in div with `data-message-id` for scroll targeting
- `src/components/ActionBar.tsx` — added search button toggle props
- `styles.css` — search-related styles + `chat-message-highlight` animation

## Notes
- One pre-existing vitest failure in `useMessageActions` (obsidian import resolution) — unrelated to this task
- Executor tasks: 0mu-a, 0mu-b, 0mu-c

## 2026-09-19 — Index-Only Startup (99860e6)

Boot now reads only `sessions/index.json` (~57ms on device for 162 sessions,
down from ~8.9s of per-file reads). Message files hydrate lazily: background
for open/active tabs, on first open otherwise.

- `JsonlStorage.loadChatData(opts?: { hydrate?: boolean })` — default
  index-only: `messages: []`, `messageCount` from index, `hydrated: false`;
  unhydrated entries tracked in `unhydratedSessions` map.
- `hydrateSession(id)` — single-file read; concurrent callers share one read
  (`pendingHydrations`); records true message ids so the next save can append.
- **Hard save guard:** an unhydrated session is never written — its index
  entry is carried forward (title/scrollPosition updates allowed). Sessions
  that gained messages unhydrated (sync `onSessionDownloaded`) write as full
  overwrite. `lastSavedState` messageIds = `null` at boot so the first write
  can never take a bad append path.
- Boot/autosave/prune filters + SessionPickerModal use `sessionMessageCount()`
  (`messageCount ?? messages.length`) so unhydrated sessions stay visible.
- Open gate (`useSessionActions.openSessionInTab`) and send gate
  (`useMessageActions.handleSend`) await hydration and **write through
  `sessionsRef` synchronously** — `TurnLifecycle` reads the ref directly.
- Full-load consumers opt into `{ hydrate: true }`: sync `_populateSyncCache`,
  pluginDataSync usage-stats, diagnostics (2 sites). Title maps stay index-only.
- Types: `ChatSession.messageCount?` / `hydrated?`. 6 new storage tests incl.
  hard-guard and concurrent-hydrate cases. Suite 506/506, tsc + build clean.
- Device-verified: `[Startup] loadChatData resolved in 57ms`; 162 sessions /
2191 messages restored. Next bottleneck identified: SyncEngine WebDAV init
~4.1s (candidate for future non-blocking init).

## 2026-09-21 — Hydration Retry + Indexed Picker Count (device-verified)

Index-only startup follow-ons, found during T42g device testing on T42g (synced sessions opened empty; later text sends in image conversations demanded an image model):

- `20ae983` — `_hydrateSessionImpl` (ChatStorage): a failed read, or a 0-message read when the index expects N, **no longer marks the session hydrated**. The session stays unhydrated, retries on next open, and logs; partial reads warn. Companion: `useSessionActions.ts` silent `.catch(() => {})` → console.error + Notice "Failed to load session messages — will retry on next open". Root cause of the permanent-empty synced session: one bad first read at boot locked the session as "hydrated" for its lifetime.
- `1fb9263` — `SessionPickerModal.tsx`: the preview line hardcoded "No messages" for any session not in memory, contradicting the (already index-backed) meta count. Now renders "N messages — open to view" from `messageCount` when `hydrated === false && msgCount > 0`. Zero file reads — the index loads once at boot. Regression test: unhydrated fixture (messageCount 3) → shows count, no "No messages".
- Verification: ChatStorage 12/12, picker 2/2, build green; Deepak device-verified 16:19 UTC ("Good. It works.").

## Open Items

- **T24a — Global search scope (⏸️ pending Deepak decision):** index-only
  startup means `useSearch.ts` only covers sessions hydrated in memory;
  unopened sessions silently return no matches (regression vs 1.5.0).
  Flagged by Codex in all three PR #8 review waves. Options: A hydrate-on-
  search (lean), B disk-backed search, C visible scope note. See
  [T24a](tasks/T24a.md).

## 2026-09-22 Sync hydration follow-up

After sync, the metadata refresh could replace loaded React sessions with
empty index-only copies. Storage could also know a downloaded file was written
while the lazy hydration map no longer contained an entry for it. The final
fix preserves loaded messages during refresh and falls back to a pure disk
peek when opening a downloaded session. Regression coverage is in
`src/hooks/__tests__/useSessionActions.test.ts`.

## 2026-09-22 Session Truncation Safety Follow-up

- 3ae26c4 added hydration start, success, empty-result, and disk-peek
  diagnostics without logging message contents.
- 7291665 made the positive-count empty-write guard independent of transient
  hydration bookkeeping. Metadata-only or partial snapshots cannot overwrite
  a non-empty JSONL payload or truncate the authoritative index.
- Session omission is no longer treated as deletion. User deletion, remote
  deletion, and clear-all paths carry explicit deletion IDs.
- Automatic retention pruning was removed from new-session creation so a
  retention setting cannot silently remove persisted history.
- Verification: 571 tests, TypeScript, and production build passed. The
  repaired build was device-tested through a 202-session download-only
  recovery with no sync errors.
