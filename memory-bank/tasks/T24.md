# T24: SessionStorage — JSONL Chat Persistence

## Task Metadata
- **ID:** T24
- **Title:** SessionStorage — JSONL Chat Persistence
- **Status:** ✅ COMPLETED
- **Priority:** HIGH
- **Started:** 2026-06-14
- **Completed:** 2026-06-14

## Description
Implement persistent chat session storage using JSONL (JSON Lines) format for human-readable, append-only, git-friendly logs. Replace or supplement in-memory state with disk-backed sessions that survive Obsidian restarts.

**2026-06-15: Search feature added on top of SessionStorage** — fuzzy search across all session JSONL files with UI integration.

## Implementation

### Phase 1: SessionStorage Core (0mu-a)
- **File:** `src/storage/session-storage.ts`
- **Class:** `SessionStorage`
- **Methods:**
  - `loadSession(id: string): ChatMessage[]` — reads `sessions/{id}.jsonl`, parses each line as JSON
  - `appendMessage(id: string, message: ChatMessage): void` — appends one JSONL line
  - `createSession(): { id: string, file: TFile }` — creates new `sessions/{uuid}.jsonl`, returns handle
- **Storage location:** `.obsidian/plugins/obsidian-ai/sessions/{uuid}.jsonl`

### Phase 2: ChatStorage Wrapper (0mu-b)
- **File:** `src/storage/ChatStorage.ts`
- **Class:** `ChatStorage` — async wrapper around SessionStorage
- **Methods:**
  - `saveSession(id, messages): Promise<void>`
  - `loadSession(id): Promise<ChatMessage[]>`
  - `listSessions(): Promise<SessionMetadata[]>`
  - `deleteSession(id): Promise<void>`
- **Integration:** Used by ChatApp for loading/saving conversation history

### Phase 3: Plugin Integration (0mu-c)
- **File:** `src/main.ts`
- **Changes:**
  - Added `sessionStorage: SessionStorage` property to `ObsidianAIPlugin`
  - Initialized in `onload()` with `app`, `manifest`, `logger` dependencies
  - Wired `ChatApp` to use `ChatStorage` for session persistence
- **Fix:** Added missing `contextPickerPathDisplay` to `ObsidianAISettings` interface (was breaking build)
- **Build:** `npm run build` passes ✅

### Migration Support
- **File:** `src/storage/Migration.ts`
- Handles migration from old in-memory format to JSONL
- **File:** `src/modals/MigrationPromptModal.ts`
- UI modal prompting user to migrate existing sessions

## Files Changed
- `src/storage/session-storage.ts` ⭐ NEW
- `src/storage/ChatStorage.ts` ⭐ NEW
- `src/storage/Migration.ts` ⭐ NEW
- `src/modals/MigrationPromptModal.ts` ⭐ NEW
- `src/main.ts` — integration + settings fix
- `src/components/ChatApp.tsx` — storage integration
- `src/context/tokenEstimator.ts` — updates for storage-aware counting
- `src/hooks/useMessageActions.ts` — updates for storage integration
- `src/settings.ts` — settings interface fix
- `src/settings-sections/advanced.ts` — advanced settings updates
- `src/agent/AgentLoop.ts` — agent loop integration
- `src/agent/OpenResponsesLoop.ts` — open responses integration

## Commit
`fdc8b58` — feat: SessionStorage with JSONL persistence + plugin integration

---

## Search Feature (Added 2026-06-15)

### Phase 4: Search Index (87z-a)
- **File:** `src/search/index.ts`
- **Class:** `SearchIndex` — in-memory inverted index from session JSONL files
- Features: stop-word filtering (70 words), posting list intersection, 200-char snippet extraction, recency sorting

### Phase 5: Search Input UI (87z-b)
- **File:** `src/components/SearchInput.tsx`
- Debounced input (300ms), clear button (×), "Search chats…" placeholder
- CSS matching Obsidian theme variables

### Phase 6: Fuzzy Search + Results Display (87z-c)
- **File:** `src/search/fuzzy-search.ts` — `FuzzySearcher` class, score threshold 0.4, highlighted match ranges
- **File:** `src/components/search-results.tsx` — results list with title badge, highlighted snippets, empty/no-results states, click-to-open-session
- Manually wired into `ChatApp.tsx` with `FuzzySearcher` ref, search state, `useEffect` for query handling

### Phase 7: UI Polish
- **CSS fix:** Garbled search results display (overlap/overflow) — title ellipsis, snippet max-height, flex containment
- **Click behavior:** Open full session + scroll to specific message + 2-second highlight animation (`chat-message-highlight` CSS)
- **Toggle visibility:** Search button in ActionBar toolbar (magnifying glass icon), hidden by default, active state styling
- **Conditional render:** Search results only show when query is non-empty (prevents chat area compression)

### Search Commits
- `ef89888` — Wired search feature (manual ChatApp integration)
- `6b90eb7` — CSS fix: search results overlap
- `d60c5cd` — Search results open full session + scroll to message
- `f620472` — Search results only show when query exists
- `503d8a7` — Search toggle button in toolbar

### Search Files Changed
- `src/search/index.ts` ⭐ NEW
- `src/search/fuzzy-search.ts` ⭐ NEW
- `src/components/SearchInput.tsx` ⭐ NEW
- `src/components/search-results.tsx` ⭐ NEW
- `src/components/ChatMessages.tsx` — wrapped MessageBubble in div with `data-message-id` for scroll targeting
- `src/components/ActionBar.tsx` — added search button toggle props
- `styles.css` — search-related styles + `chat-message-highlight` animation

## Notes
- One pre-existing vitest failure in `useMessageActions` (obsidian import resolution) — unrelated to this task
- Executor tasks: 0mu-a, 0mu-b, 0mu-c
