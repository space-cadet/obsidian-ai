 ---
 source_branch: claude/continue-next-task-9iSgD
 source_commit: a3171ff11bda15ce4e845ef78cd0ca1729bc6907
 ---

# T7: Release System & CI/CD
*Created: 2026-05-02 09:34:00 IST*
*Last Updated: 2026-05-02 09:34:00 IST*

**Description**: Establish a two-track release pipeline — tag-triggered stable releases (existing) and automatic pre-release builds on every push to `main` for immediate testing. Fix the stale `versions.json` entry.
**Status**: ✅ Completed
**Priority**: HIGH
**Started**: 2026-05-02
**Last Active**: 2026-05-02 09:34:00 IST
**Dependencies**: —

## Completion Criteria
- ✅ `versions.json` updated to include `1.2.4` entry
- ✅ `.github/workflows/pre-release.yml` created — triggers on push to `main`
- ✅ Pre-release builds tagged `latest-dev`, marked `prerelease: true`, artifacts replaced on each push
- ✅ Existing `release.yml` (tag-triggered) unchanged
- ✅ Implementation doc created

## Related Files
- `versions.json`
- `.github/workflows/pre-release.yml`
- `.github/workflows/release.yml` (unchanged)
- `memory-bank/implementation-details/release-ci-design.md`

## Progress
1. ✅ Fix `versions.json` — add `1.2.4` entry
2. ✅ Create `.github/workflows/pre-release.yml`
3. ✅ Create implementation doc

## Context
The existing `release.yml` already correctly builds and publishes on any tag push. The new `pre-release.yml` mirrors it but targets the `main` branch, creates a rolling `latest-dev` tag (overwritten each push via `allowUpdates: true` + `removeArtifacts: true`), and marks the release as a pre-release so it does not surface as the latest stable version to plugin users.
