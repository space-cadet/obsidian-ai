# T67: Storage Lifecycle Decomposition

## Status: IN PROGRESS

Extract the monolithic `src/lifecycle/storage.ts` into focused, testable
slices with clear ownership boundaries.

## Current Boundaries

| # | Module | Responsibility | Status | Tests |
|---|--------|---------------|--------|-------|
| 1 | `persistence.ts` | Settings load/save, chat-data read/write | Complete | 18 |
| 2 | `sync.ts` | Sync engine init, trigger, rebuild, cancel | Complete | — |
| 3 | `pluginDataSync.ts` | Plugin-data sync (settings, memory, persona, usage) | Complete | 11 |
| 4 | *(remaining)* | Initialization, migration, session-end intelligence, cleanup | Current | — |

## Remaining Work

`storage.ts` still contains ~200 lines spanning three concerns:

1. **`initializeStorage`** — plugin bootstrap (logger, settings, registry,
   chatapi, sessionStorage, personaLoader, searchIndex, sessionSummarizer,
   chatStorage, migration prompt)
2. **`migrateStorage`** — legacy ID directory copy
3. **`onSessionEnd`** — session-end summarization + memory curation
4. **`cleanupStorage`** — logger shutdown

The initialization function is the largest remaining block. Options:
- Keep as-is (it's a coordinator, not a domain owner)
- Extract `initializeIntelligence` for persona/search/summarizer setup
- Extract `initializeChatStorage` for storage + migration prompt

## Verification

- [x] TypeScript compilation
- [x] Production build
- [x] All tests (29 lifecycle tests)
- [x] `git diff --check`

## Commits

- `0974157` — refactor(lifecycle): extract persistence into persistence.ts
- `2995047` — refactor(lifecycle): extract sync orchestration into sync.ts
- `56422f1` — refactor(lifecycle): extract plugin data sync into pluginDataSync.ts
