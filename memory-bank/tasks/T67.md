# T67: Storage Lifecycle Decomposition

*Last Updated: 2026-08-30 16:46:02 IST*

## Status: IN PROGRESS

Extract the large `src/lifecycle/storage.ts` file into focused, testable
slices with clear ownership boundaries.

## Current Boundaries

| # | Module | Responsibility | Status | Tests |
|---|--------|---------------|--------|-------|
| 1 | `src/lifecycle/persistence.ts` | Settings and chat-data load/save, queued writes, backups, and auto-sync scheduling | Complete | 18 |
| 2 | `src/lifecycle/sync.ts` | Sync engine setup, manual sync, index rebuild, and cancellation | Complete | — |
| 3 | `src/lifecycle/pluginDataSync.ts` | Selected settings, memory, persona, audit, and usage-data sync | Complete | 11 |
| 4 | `src/lifecycle/storage.ts` | Startup, migration, session-end memory work, and logger cleanup | Current | — |

## Remaining Work

`storage.ts` now contains 227 lines and four remaining coordinator functions:

1. **`initializeStorage`** — plugin bootstrap (logger, settings, registry,
   chatapi, sessionStorage, personaLoader, searchIndex, sessionSummarizer,
   chatStorage, migration prompt)
2. **`migrateStorage`** — legacy ID directory copy
3. **`onSessionEnd`** — session-end summarization + memory curation
4. **`cleanupStorage`** — logger shutdown

The initialization function is the largest remaining block. Options:
- Keep as-is (it's a coordinator, not a domain owner)
- Extract `initializeIntelligence` for persona/search/summarizer setup
- Extract `initializeChatStorage` for storage + migration prompt

## Verification

- [x] TypeScript compilation
- [x] Production build
- [x] Focused lifecycle tests (29/29)
- [x] Full test suite (48 files / 433 tests)
- [x] `git diff --check`
- [ ] Prettier check (28 files still need formatting)

## Commits

- `0974157` — refactor(lifecycle): extract persistence into persistence.ts
- `2995047` — refactor(lifecycle): extract sync orchestration into sync.ts
- `56422f1` — refactor(lifecycle): extract plugin data sync into pluginDataSync.ts
- `ac0201f` — docs(memory-bank): update T67 progress and session cache
- `fa35c12` — docs(memory-bank): add T67 boundary 3 session log

## Progress — 2026-08-30

- Persistence was moved to `persistence.ts`, with 18 focused tests.
- Sync setup and actions were moved to `sync.ts`.
- Plugin-data sync and its 11 focused tests were moved to
  `pluginDataSync.ts`.
- The original `storage.ts` exports remain available so existing callers do
  not break.
- The source changes are pushed to `origin/main` at `fa35c12`; these Memory
  Bank updates are currently local.

## Remaining Decision

Decide whether the remaining startup and session-end code should stay in
`storage.ts` as coordinator code or be split into smaller modules. Do not
split it only to reduce the line count; first identify a clear responsibility
and add tests around it.
