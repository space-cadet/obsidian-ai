# T58: Integrate Sync UI into Chat Lab

*Created: 2026-08-20*
*Status: 🔄 IN PROGRESS*
*Last Updated: 2026-08-23 01:27:43 IST*
*Parent: T42 (Remote Chat Storage & Sync)*
*Scope: UI/UX Redesign — Move sync from separate sidebar into Chat Lab*
*Migration: Canonical replacement for the sync records previously stored under T43.*

## Objective

Replace the standalone sync sidebar (`SyncSidebarView`) with an integrated sync experience inside the Chat Lab sidebar. Sync becomes a first-class tab alongside chat sessions, accessible via a dropdown from the export button.

## Background

Commit `eebcfb6` (T42f) introduced a separate `SYNC_SIDEBAR_VIEW_TYPE` sidebar for sync progress. This creates two sidebar views for one plugin, which is poor UX. The sync UI also lacks the rich display patterns proven in SyncIt (per-file log entries, progress cards, mode selector).

**T42f is superseded by this task.** The existing `SyncSidebarView.ts` code will be removed; its patterns will be ported into a new `ChatSyncPanel` component rendered inside chat tabs.

## Acceptance Criteria

- [x] **Remove 2nd sidebar**: `SYNC_SIDEBAR_VIEW_TYPE` registration, `activateSyncSidebar()`, ribbon icon, related commands deleted
- [x] **Export dropdown**: ActionBar export button opens dropdown with "Export" and "Sync" options
- [x] **Sync tab**: Clicking "Sync" opens a special non-session tab in `ChatTabBar` (label: "🔄 Sync")
- [x] **Sync panel**: Rich sync UI rendered when sync tab is active (replacing `ChatMainArea` for that tab)
- [x] **Direction control**: Dropdown in sync panel: Two-way / Upload only / Download only. Defaults to settings value
- [x] **Settings default**: `remoteStorage.syncDirection` setting added ("both" | "upload" | "download"), default "both"
- [x] **Dry run**: Toggle/button in sync panel to run dry-run sync
- [x] **Rich progress**: Per-session log entries with color-coded badges (uploaded, downloaded, conflict, skipped)
- [x] **Completion summary**: Cards showing sync results with icons and colors
- [x] **Cancel**: Working cancel button during active sync
- [x] **Debug logging**: All sync operations logged via existing `FileLogger` (no separate log tab)
- [x] **Settings access**: Sync settings remain in main Settings panel (no change needed)

## Out of Scope (for T58)

- Manual conflict reconciliation UI (skip per user request)
- Separate log tab (operations go to debug log per user request)
- S3 backend (T42 Phase 3)
- Custom server backend (T42 Phase 4)

## Architecture

### Component Hierarchy

```
ChatApp
├── ChatToolbar (ActionBar)
│   └── ExportDropdown → opens menu with Export / Sync
├── ChatTabBar
│   ├── Session tabs (existing)
│   └── 🔄 Sync tab (new, special non-session tab)
└── ChatMainArea
    ├── Regular session content (existing)
    └── ChatSyncPanel (new, when sync tab active)
        ├── Direction selector
        ├── Dry run toggle
        ├── Status display
        ├── Progress bar + stats
        ├── Per-session log list
        └── Action buttons (Sync, Cancel)
```

### State Flow

```
User clicks Export dropdown → "Sync"
  → openSyncTab() adds "__sync__" to openSessionIds, sets activeSessionId = "__sync__"
  → ChatMainArea detects sync tab → renders ChatSyncPanel instead of ChatMessages
  → ChatSyncPanel calls plugin.triggerSync(direction, dryRun) when user clicks Sync
  → SyncEngine runs with given direction
  → Progress callbacks update ChatSyncPanel state
  → Results displayed as completion cards
```

### Key Files

| File | Action | Purpose |
|------|--------|---------|
| `src/components/ChatSyncPanel.tsx` | **Create** | Main sync panel component |
| `src/components/presentational/ExportDropdown.tsx` | **Create** | Dropdown menu for export/sync actions |
| `src/components/presentational/SyncLogEntry.tsx` | **Create** | Individual sync log item with badge |
| `src/components/presentational/SyncCompletionCards.tsx` | **Create** | Result summary cards |
| `src/hooks/useChatSync.ts` | **Create** | Hook for sync state management |
| `src/components/ChatApp.tsx` | **Modify** | Add sync tab logic, wire dropdown |
| `src/components/ChatTabBar.tsx` | **Modify** | Handle special "__sync__" tab |
| `src/components/ChatMainArea.tsx` | **Modify** | Render ChatSyncPanel for sync tab |
| `src/components/presentational/ActionBar.tsx` | **Modify** | Replace export button with dropdown trigger |
| `src/main.ts` | **Modify** | Remove SyncSidebarView registration, keep sync engine |
| `src/settings.ts` | **Modify** | Add `syncDirection` to RemoteStorageConfig |
| `src/sync/SyncEngine.ts` | **Modify** | Accept direction parameter in sync() |
| `src/ui/SyncSidebarView.ts` | **Delete** | Replaced by ChatSyncPanel |

## Implementation Phases

### Phase 1: Remove 2nd Sidebar ✅
- Delete `SYNC_SIDEBAR_VIEW_TYPE` registration from `main.ts`
- Delete `activateSyncSidebar()`, `getSyncSidebarView()`, `registerView` for sync sidebar
- Delete sync ribbon icon and "Open Chat Sync" command
- Remove `src/ui/SyncSidebarView.ts` (or deprecate)

### Phase 2: Add Settings Default ✅
- Add `syncDirection: "both" | "upload" | "download"` to `RemoteStorageConfig`
- Add to `DEFAULT_SETTINGS`, `normalizeSettings`
- Add direction selector to sync settings section

### Phase 3: Export Dropdown + Sync Tab ✅
- Create `ExportDropdown.tsx` component
- Modify `ActionBar.tsx` to use dropdown instead of direct export button
- Add `openSyncTab()` / `closeSyncTab()` to `useSessionActions`
- Modify `ChatTabBar` to render "🔄 Sync" tab for `__sync__` ID
- Handle tab close for sync tab

### Phase 4: ChatSyncPanel ✅
- Create `ChatSyncPanel.tsx` with:
  - Direction selector dropdown (reads default from settings)
  - Dry run toggle
  - Status area (idle / syncing / complete / error)
  - Progress bar + stat counters
  - Per-session log list (scrollable)
  - Sync / Cancel / Settings buttons
- Port rich UI patterns from SyncIt:
  - Color-coded badges for operations
  - Completion summary cards
  - File-size formatting

### Phase 5: SyncEngine Direction Control ✅
- Modify `SyncEngine.sync(direction?)` to accept optional direction override
- Filter `computeSyncPlan()` results based on direction
- Ensure dry-run respects direction

### Phase 6: Integration & Testing ✅
- Wire `ChatApp` to render `ChatSyncPanel` when sync tab active
- Test sync flow end-to-end
- Verify debug logging works
- Build and test in Obsidian

## Subtasks (Post-Delivery Fixes)

Discovered during tablet testing on 2026-08-21:

| Subtask | Issue | Status |
|---------|-------|--------|
| [T58a](tasks/T58a.md) | Fix rebuildSyncIndex title resolution (shows "Untitled Session") | ✅ Complete |
| [T58b](tasks/T58b.md) | Add activity indicators (spinners/animated feedback) to sync UI | ✅ Complete; follow-up in T58d |
| [T58c](tasks/T58c.md) | Extend sync to all plugin data (settings, memory, logs, index) | ✅ Complete |
| [T58d](tasks/T58d.md) | Unified progress, dry-run planning, and index rebuild UX/performance | 🔄 Planned |

## Design References

- SyncIt `SyncSidebarView.ts` — patterns for rich progress display, per-file logs, completion cards
- T42f code (`src/ui/SyncSidebarView.ts`) — existing obsidian-ai sync UI (to be ported)
- `src/sync/SyncEngine.ts` — existing engine (needs direction parameter)

## Related Tasks

- T42 (parent): Remote Chat Storage & Sync
- T42a–f (superseded by T58 for UI portion): Phase 6 SyncIt ports
- T15: Tabbed Chat Interface (session tabs infrastructure)

## Notes

### 2026-08-20–21 Sync panel completion
- Added compact SyncIt-style sync panel layout with a scrollable Files area.
- Added session titles, direct Remote Storage settings shortcut, and compact actions.
- Added rebuild choices: trust remote, trust local, compare, or cancel.
- Rebuild actions now update the sync record, stream file operations into the Files list, show results, and support cancellation.
- Added a close button to the special Sync tab.
- Commits: `6d84ced`, `8cdfb80`, `71467fa`, `97689f9`, `b31f6bd`, plus the Sync-tab close fix in this session.

- The `SyncProgressModal.ts` can be kept as an optional fallback for users who prefer modal-style progress
- Debug logging goes through existing `FileLogger` → no separate log infrastructure needed
- Sync settings UI in SettingsTab remains unchanged — this task is purely about the in-chat sync experience
- T58d's implementation now covers visible planning, stable progress rows,
  dry-run plugin-data planning, persistent completion, and chat-session index
  rebuild progress. The follow-up audit confirms that plugin data is not part
  of the Rebuild action and needs a separate state-rebuild phase if required.
