# T2: Conversation Chain & Memory
*Created: 2026-05-02 08:13:57 IST*
*Last Updated: 2026-05-03 00:45:00 IST*

---
source_branch: claude/fix-note-editing-context-umpuw
source_commit: df4130cf7f7253e31d572b6bdbb787f48cdc59c4
---

## Description

Replace the current single-turn `callApi()` pattern with a stateful multi-turn conversation chain. Full message history is built and sent to the LLM on every turn. Conversations are persisted to disk and can be loaded in future sessions.

## Status
🔄 IN PROGRESS

## Priority
HIGH

## Dependencies
- T1 (Chat Panel) — conversation state is displayed in the chat panel

## Completion Criteria

- ✅ Define `ChatMessage` and `ChatSession` TypeScript interfaces
- ✅ Implement session-based persistence (plugin methods, not standalone class):
  - ✅ `loadChatData()` — load `StoredChatData` from plugin storage with migration
  - ✅ `saveChatData(data)` — persist sessions + activeSessionId
  - ✅ Archive-on-New behaviour in ChatApp
  - ✅ `deleteSession(id)` — remove from history via SessionPickerModal
  - ✅ `pruneSessions(sessions, max)` — respect `maxSavedConversations`
- ✅ `streamChat(messages, signal)` on `ChatApiManager` (completed in T4)
- ✅ Wire session store into `ChatApp` submit flow (update `updatedAt` on each turn)
- ✅ Persist max 20 sessions; prune oldest on overflow
- ✅ Auto-title session from first user message (truncated to 40 chars)
- ✅ Load Session modal (not dropdown) in ActionBar — list with title, date, count, preview
- ✅ Archive-on-New: save current session before clearing
- ✅ Existing `callSelection()` (inline tooltip) is unchanged

## Related Files

- `src/main.ts` — `loadChatData`, `saveChatData`, migration from flat array
- `src/views/ObsidianAIChatView.ts` — extend `ChatPluginLike` interface
- `src/components/ChatApp.tsx` — session state, archive-on-New, save-on-message
- `src/components/ActionBar.tsx` — enable Load button, open SessionPickerModal
- `src/components/SessionPickerModal.tsx` (new) — session list modal
- `memory-bank/implementation-details/chat-panel-design.md`
- `memory-bank/implementation-details/chat-session-persistence.md` (new)

## Data Model

```typescript
interface ChatMessage {
  id: string
  role: "user" | "assistant"
  content: string
  timestamp: number
  isError?: boolean
}

interface ChatSession {
  id: string
  title: string
  createdAt: number
  updatedAt: number
  messages: ChatMessage[]
}

interface StoredChatData {
  sessions: ChatSession[]
  activeSessionId: string | null
}
```

## Progress

1. ✅ Define types (ChatMessage, ChatSession, StoredChatData) — extracted to `src/types.ts`
2. ✅ Basic single-session persistence via `plugin.loadChatMessages()` / `saveChatMessages()` — messages saved on every update, loaded on mount, cleared on New Chat. Lives in `main.ts`; saveSettings now merges rather than replaces plugin data.
3. ✅ Design session-store architecture — `StoredChatData` with `sessions[]` + `activeSessionId`; plugin API methods spec'd; SessionPickerModal UI designed; auto-titling and pruning logic defined
4. ✅ Implement `loadChatData` / `saveChatData` in plugin with migration from flat `chatMessages`
5. ✅ Update `ChatApp` to session state: archive-on-New, save-on-message, activeSessionId tracking
6. ✅ Build `SessionPickerModal` component: list, preview, load, delete
7. ✅ Update `ActionBar`: enable Load button when history exists, open modal on click
8. ✅ Auto-titling: first user message truncated to 40 chars
9. ✅ Pruning: respect `maxSavedConversations` setting on archive
10. ⬜ Real-world testing in Obsidian (migration, pruning, delete-active-session edge case)

## Context

The `MessageQueue` in the current codebase is UI-only (arrow key navigation of past prompts). It is not replaced — it stays in the inline tooltip. The `ConversationManager` is a new separate concern for the chat panel only. The full LangChain message array (all turns) is passed to `streamChat()` on every submit, giving the LLM complete conversation context.
