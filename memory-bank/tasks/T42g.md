---
task_id: T42g
title: Sync Visibility & Observability (progress UI, stats, logs)
status: Complete (device-verified 2026-09-21)
priority: HIGH
parent: T42
depends_on: [T42, T55, T58d]
created: 2026-09-19
last_updated: 2026-09-24
---

# T42g: Sync Visibility & Observability

## Goal

Make sync fully legible to the user: live transfer progress (bytes + derived rates), per-file activity, structured errors, conflict/overwrite detail, and browsable history — across **all** trigger paths (sidebar panel, auto-sync-on-save, settings Sync Now, command palette, startup init).

Directed by Deepak 2026-09-19 (blocking v1.6.0): "when it runs it doesn't show the user any information. What is being downloaded, uploaded, overwritten. Transfer rates, stats, logs. Nothing."

## Relationships

- Parent **T42** (Remote Chat Storage & Sync). Siblings T42a–d (index, atomic writes, concurrency, server signature).
- **T58d** (Unified Sync Progress/Dry-Run/Rebuild UX) built the per-operation row progress (pending→active→done/skipped/error, shimmer, sticky 100% bar) — T42g builds observability on top of that foundation, not in place of it.
- Distinct from **T42e** (dry-run mode) and **T42f** (⛔ superseded standalone sidebar, replaced by T58).
- Reference implementation: `code/obsidian-syncit` (space-cadet), observability UI shipped 2026-09-01 (`156a125`).

## Background — audit findings (2026-09-19)

What exists: `triggerSync` (src/lifecycle/sync.ts) is fully instrumented — per-session logs with titles, `SyncProgressSnapshot` (counts + elapsedMs), `SyncLogger` (local file + remote append), final summary toast. `ChatSyncPanel` (v2) is the only live UI.

Gaps:

1. **Every non-panel path is toast-only.** Auto-sync after save (debounced 3s — the most common path), settings Sync Now (`remoteStorageSettings.ts:377`), command palette (`registration.ts:149`) call `triggerSync()` with no callbacks. No running indication; final 6s toast only.
2. **Auto-sync failures are silent** — swallowed to the file logger (`scheduleAutoSync` catch in `persistence.ts`). User never sees a failure.
3. **No bytes, no rates.** `SyncProgressSnapshot` tracks counts only. Byte/rate data does not exist in the data model.
4. **Overwrites are opaque.** Conflicts counted (⚡n) but no per-conflict resolution detail (which side won, what was overwritten).
5. **No in-app log viewer.** `SyncLogger` history is written to files; nothing in-app reads it back.
6. **Startup init is silent** — ~4.1s WebDAV init (T24 notes) blocks startup with zero feedback.
7. **`SyncProgressModal` is dead code** — `useModal: true` never passed by any caller. Wire it or delete it.

## Reference patterns from obsidian-syncit

Port patterns, not verbatim code:

- **Byte accounting**: `SyncPlan.uploadSize/downloadSize` (planned, from `FileEntity.size`), `SyncResult.uploadedBytes/downloadedBytes` (actual). Rates derived = bytes ÷ elapsed.
- **Structured failures**: `SyncOperationFailure{operation: upload|download|conflict|local-delete|remote-delete, path, phase: transfer|delete, message, details}` — persisted, drives an errors surface.
- **Reconciliation model**: `ReconciliationItem` + `ReconciliationReason` (no-baseline-local-only/remote-only/conflict, possible-remote/local-deletion) + `ReconciliationDecision` (use-local/use-remote/keep-both/skip). Answers "what's being overwritten."
- **SyncLogger**: leveled + categorized, async queued flush, rotation by max age (days) AND max size (MB), backup in plugin dir, tested. obsidian-ai's logger has no rotation.
- **Five-surface sidebar blueprint**: sync (controls) / activity (recent ops) / errors (failures) / reconciliation (decisions) / progress (bar + meta).

Constraint in both codebases: no streaming Content-Length hooks in either WebDAV adapter → "live KB/s mid-transfer" is NOT free; totals + derived rate are the honest v1. Real-time throughput needs new adapter work (defer unless Deepak insists).

## Design-first workflow (Deepak-directed)

1. **Luna generates HTML mockups** → `scratch/t42g-mockups/` (self-contained, inline CSS).
2. **Cloudy screenshots + reviews + refinement rounds** (playwright), critique fed back via sessions_send.
3. **Deepak sign-off on visual quality** before implementation.
4. Implement: panel rebuild + status-bar indicator + engine/snapshot/logger changes.
5. Tests (extend existing sync/panel suites), build, device verify.

## Scope

- **P1** — Status-bar sync indicator (Obsidian `addStatusBarItem`): idle / syncing ↑n ↓n / error / last-sync; fed at engine level so it covers every trigger path; click → panel.
- **P1** — Failure surfacing: auto-sync errors visible (status-bar error state + click-to-retry), not file-log whispers.
- **P2** — Byte counters: adapter + snapshot += uploadedBytes/downloadedBytes (+ planned sizes); totals + derived rate in panel.
- **P2** — Conflict detail: resolution + explicit "overwritten" marking per conflict; post-run conflicts section.
- **P2** — Sync log viewer: in-app view of SyncLogger history (last N sessions, filter by direction/status).
- **P3** — Startup "Sync: connecting…" status during engine init.
- **Cleanup** — wire or delete orphaned `SyncProgressModal`.

## Files (expected)

`src/sync/SyncTypes.ts` (snapshot += bytes/failures/reconciliation), `src/sync/SyncEngine.ts`, `src/sync/SyncLogger.ts`, `src/sync/WebDAVStorageAdapter.ts` (size hooks), `src/components/ChatSyncPanel.tsx` (rebuild), `styles.css`, status-bar registration (`src/main.ts` / `src/ui/registration.ts`), `src/components/SyncProgressModal.ts` (wire or delete).

## Implementation Log (2026-09-20, after Deepak approved Luna mockups r4)

Branch `main`, 3 commits (Cloudy):

- `0720540` — **Byte accounting** (SyncIt data-model port): `SyncPlan.uploadBytes/downloadBytes` + `estimateSessionBytes()` in StorageAdapter; engine emits `planBytes` on the plan stage and per-op `bytes` on done events (payload size upload / remote size download); direction filter recomputes plan bytes; `SyncProgressSnapshot` gains `uploadedBytes/downloadedBytes/plannedUploadBytes/plannedDownloadBytes`; new `SyncOperationFailure` interface. Tests: `SyncEngine.planBytes.test.ts` (3).
- `e604eed` — **SyncStatusHub** (`src/sync/SyncStatusHub.ts`): app-wide bus — state (`disabled/idle/connecting/syncing/error`), progress, runLog (merge by id, cap 300), structured failures extracted from error log entries, `lastResult`. ALL entry points publish: `triggerSync` (success + catch), `rebuildSyncIndex` (success + catch), `initSyncEngine` (connecting/idle/error/disabled). Auto-sync passes `trigger: "auto"`. **App status-bar indicator** (`src/sync/SyncStatusBar.ts`): idle = last-sync relative time + bytes; syncing = counts + derived rate; error = "tap to retry"; click activates Chat Lab. Tests: `SyncStatusHub.test.ts` (6).
- `bfd525b` — **Panel rebuild** (`ChatSyncPanel.tsx` ~430 lines, hub-driven): off / idle / syncing / complete surface (stats grid + full op log w/ chips + Show all N) / conflicts & errors (failures card + Retry). Progress bar (determinate + indeterminate), active-op shimmer rows, activity feed tab with fade. **SyncLogger rotation**: age (14d) + line (1000) + byte (512KB) pruning, single backup slot (`sync.log.1`), `readRecent()` + `SyncLogModal` in-app viewer ("View sync log"). Command palette gains "Chat Sync: Sync Now" with `useModal: true` (dead modal now has a real caller). CSS: ~510 lines (`sync-*`, Obsidian theme vars, no hardcoded light values). Tests: `ChatSyncPanel.test.tsx` (6) + `SyncLogger.test.ts` (5). Suite 537/537, tsc clean.

### Deliberately deferred (recorded honestly)

- **Per-conflict decision chips** (mockup r4 showed use-local/use-remote/keep-both): needs a reconciliation-decision store consulted by `computeSyncPlan` (SyncIt `ReconciliationItem` port) — engine work, not UI. Conflicts currently surface as structured "overwritten" rows with counts. Follow-up task candidate.
- **Live mid-transfer KB/s**: adapters have no streaming hooks; rates are derived (bytes ÷ elapsed), per audit conclusion.
- **Plugin-data file bytes**: session transfers report bytes; the 5 plugin-data files still count as operations only.
- **Log viewer is read-only plain text** (last 200 lines); filtering/history-per-run = later.

## Acceptance criteria

- [x] Syncing state visible from every trigger path (status bar + panel)
- [x] Auto-sync failure produces a visible, actionable signal (status-bar error + Retry)
- [x] Panel shows bytes (planned + transferred) and derived rate for the run
- [~] Conflicts show counts + "overwritten" marking post-run; per-conflict which-side-won detail = partial (decision store deferred)
- [x] Sync history browsable in-app (SyncLogModal, last 200 lines; run-scoped log in panel)
- [x] No dead modal code (wired: command palette "Chat Sync: Sync Now" uses modal)
- [x] Tests extended; full suite 537/537 + tsc green
- [x] Deepak device-verifies visual quality (aankhon dekhi) — ✅ 2026-09-21 ("Good. It works." — synced sessions hydrate on open, history counts accurate)

## 2026-09-21 follow-ups (2 commits, morning session)

- `8fafd34` — Settings "Sync Now" button (`remoteStorageSettings.ts:377`) passes `useModal: true`; the progress modal now shows from Settings (command palette already had it via `bfd525b`). Auto-sync-on-save intentionally stays modal-less — the status-bar indicator covers the ambient path.
- `5749148` — **critical**: `emitLog` self-recursion. Two helper definitions in `src/lifecycle/sync.ts` (~L197 rebuild path, ~L409 triggerSync path) called `emitLog(entry)` unconditionally → synchronous infinite recursion → `RangeError: Maximum call stack size exceeded` on **every** sync op (upload/download/conflict), surfaced through SyncEngine per-session catches (:352/:390/:432). Introduced by hub-wiring commit `e604eed`. Fixed to `options?.onLog?.(entry)` at both sites. Unit suites missed it: the bug lives in lifecycle wiring, not hub/engine internals, and `emit()` swallows listener errors.

Device verification 2026-09-21 (Deepak, T42g): "Good. It works." Panel styles: the T42g CSS block was relocated from the hand-appended `styles.css` tail into source partial `styles/_sync.css` (build-safe home; working tree, uncommitted as of this record — see T66 addendum).

## 2026-09-22 follow-up

Commits `5f0818b`, `a1ac16b`, `2bf4ab2`, `ef43637`, and `4ce4b07` completed
the next observability and correctness pass: live titles and counters,
deletion-plan visibility, rebuild-index feedback, plain metadata titles,
history publication, batched persistence, visible post-download disk/index/
manifest stages, and a timer that starts at Sync confirmation. Full automated
verification now passes 67 files / 568 tests plus the production build.

## 2026-09-24 release decision

- Automatic sync is disabled for 1.6.0 until it is verified. Manual Sync Now
  remains available; do not report automatic sync as a supported release path.
