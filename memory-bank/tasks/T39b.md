# Task: T39b
*Created: 2026-08-05 17:28:52 IST*
*Last Updated: 2026-09-19 03:15 IST*

## Task Information
**Title:** Git Write Tools from Chat (stage, commit, pull, push)
**Status:** ✅ COMPLETE
**Priority:** HIGH
**Created:** 2026-08-05 17:28:52 IST
**Started:** 2026-09-18
**Completed:** 2026-09-18

## Description

Deepak, 2026-09-18: "Of course I want git write tools. How will obsidian-ai
use the git plugin without them?" This is the point of the git integration
(T39). The earlier "hold write tools until a safety framework exists" plan
was AI-invented and is dead (T38 cancelled same day) — the only gate is
obsidian-ai's existing auto-execute toggle.

## Acceptance Criteria

- [x] `git.stage(paths)` — explicit vault-relative paths only; missing
      paths stage as deletions; no stage-all
- [x] `git.commit(message)` — commits the staged state, message required,
      returns the commit hash; author from Git Sync settings
- [x] `git.pull()` — uses the plugin's existing pull/auth path
- [x] `git.push()` — uses the plugin's existing push/auth path
- [x] obsidian-ai registers write-risk provider tools (read-only hard-block
      removed); the existing toggle prompts per call when off
- [x] CI green (492/492 tests, tsc clean, end-to-end smoke: stage incl.
      deletions, commit hash matches HEAD, snapshot cache invalidation)

## Notes

- Credentials and auth stay in obsidian-git; the provider only wraps
  operations the plugin already performs for its own UI.
- Explicit paths first. Broader "commit everything" conveniences only if
  Deepak asks.
- Verified via /tmp/isotest/smoke6.cjs (17 checks, all passing).
