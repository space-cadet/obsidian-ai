# Task: T39a
*Created: 2026-08-05 17:28:52 IST*
*Last Updated: 2026-09-19 03:55 IST*

## Task Information
**Title:** Provider API Host, Lifecycle, and Tool-Policy Boundary
**Status:** ✅ COMPLETE — provider tools register and execute end-to-end
**Priority:** HIGH
**Created:** 2026-08-05 17:28:52 IST
**Started:** 2026-08-05 17:48:15 IST
**Completed:** 2026-09-18

## Description

Implement Obsidian AI's host side of the Integration Provider API: a stable
registry for installed peer plugins, capability discovery, tool availability,
host-enforced approval/audit policy, and typed result/progress handling.

## Acceptance Criteria

- [x] Define runtime-validated provider/capability contracts with explicit API
  version and risk classification.
- [x] Discover compatible providers at plugin load and layout readiness.
- [x] Register enabled namespaced read-only tools and report incompatibility.
- [x] Route read-only provider calls through ToolExecutor and the tab-scoped
  approval/result loop.
- [x] Provide opt-in Integrations settings without credential display.
- [ ] Render provider calls through generic pending-operation and inline
  progress/result cards, including redacted typed previews and clear provider
  unavailability feedback.
- [ ] Show the active tool-policy state compactly in the chat UI once T38
  replaces the current binary auto-apply setting.
- [ ] Cover version negotiation, duplicate names, provider loss, policy
  classification, and rejected/failed execution with focused tests.
- [ ] Expose the resolved registry through OpenResponses and test conversion,
  built-in/provider ID collisions, local schema rejection, and availability refresh.

## Implementation Details

- Prefer a documented peer-plugin API and a host adapter/registry over dynamic
  loading of third-party bundles within Obsidian AI.
- Keep provider descriptors JSON-serializable at the model boundary. Runtime
  execute/progress callbacks stay inside the trusted Obsidian plugin process.
- The host owns final risk classification. Provider declarations inform UI and
  compatibility but cannot bypass confirmation for a remote or destructive
  operation.
- The first UI slice comprises an Integrations settings list and generic cards;
  it must not duplicate a provider's normal configuration or manual UI.

## Related Files

- `src/agent/tools.ts`: built-in registry integration point.
- `src/agent/ToolExecutor.ts`: execution dispatch integration point.
- `src/agent/types.ts`: tool-call/result types to extend without breaking
  built-in tools.
- `src/hooks/useMessageActions.ts`: approval card wiring.
- `src/settings-sections/integrations.ts`: planned provider availability and
  enablement view.
- `src/components/PendingToolCard.tsx`: generic provider approval preview.
- `src/components/ToolCallNotification.tsx`: inline progress and result view.
- `memory-bank/implementation-details/integration-provider-api.md`: contract.

## Dependencies

- **Depends On:** T39, T13, T38
- **Blocks:** T39b

## Progress Tracking

- 2026-08-05 17:28:52 IST: Planned as the host-side first delivery stage; no
  code changes authorized yet.
- 2026-08-05 17:39:15 IST: Added the UI contract and ASCII wireframes to the
  canonical implementation document; no code changes authorized yet.
- 2026-08-05 17:48:15 IST: Added registry, opt-in settings, read-only tool
  composition/dispatch, generic provider labels, and focused tests. `pnpm
  test` passed 158 tests and `pnpm run build` passed.
- 2026-08-25 12:52:36 IST: Linked remaining host work to T60a–c after the
  tool-system audit identified OpenResponses registry and validation gaps.

## Issues and Blockers

- Awaiting the shared approval and audit work tracked by T38 for mutation
  capability delivery. **Resolved 2026-09-18 by cancellation:** T38 was an
  AI-authored task Deepak never requested; he rejected it ("bullshit put in
  there by GPT") and it is recorded as ❌ CANCELLED. The existing
  auto-execute toggle is the only gate, so the mutation-delivery blocker
  vanished with T38 itself.

## Closeout — 2026-09-18

- `a5186ef` fixed the registration crash: `providerCapabilityToToolDefinition()`
  passed a bare JSON-schema object to the AI SDK `tool()`, which tried to call
  it as a function while building the model request — the source of the
  user-visible `TypeError: t is not a function` the moment any provider tool
  was registered. Schemas now normalize through `toModelInputSchema()`
  (zod/~standard/SDK-wrapped pass through; bare JSON-schema objects get the
  `jsonSchema()` wrapper). The raw schema stays on the descriptor for
  host-side Ajv validation and OpenResponses conversion. Regression test
  exercises the exact `asSchema()` crash site.
- Write-tool registration moved to T39b (`19f1a37`), which dropped the
  read-only hard-block; the auto-execute toggle gates every call.
- Criteria rendered moot by T38's cancellation (tool-policy state UI, T38-gated
  mutation delivery) are dead with T38 and are not carried forward. The
  OpenResponses registry exposure criterion was satisfied under T60a.
- Verified: 492/492 tests, tsc clean, 17/17 end-to-end smoke checks with the
  obsidian-git provider (deletion staging, commit hash == HEAD, cache
  invalidation).

## Notes

- Built-in note tools remain supported and do not need to be rewritten as
  providers in the first iteration.
- The host-side provider registry remains an AI tool boundary. T57d's future
  `dataSyncProvider` contract is separate and must not grant arbitrary file
  writes through this registry.
