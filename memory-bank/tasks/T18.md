# T18: Web Search Tool for Chat
*Created: 2026-05-16 23:30 IST*
*Last Updated: 2026-05-16 23:30 IST*

---
source_branch: main
source_commit: HEAD
---

## Description

Add a `search_web` tool to the agentic tool suite, allowing the AI to search the web for current information. The user can choose between three search providers in Settings: DuckDuckGo (free, no API key), Brave Search API (requires key, 2000 free queries/month), or SearXNG (self-hosted).

## Status
✅ **COMPLETE** — Implemented and builds successfully.

## Phases

| Phase | Description | Status |
|-------|-------------|--------|
| 1 | Tool definition (`searchWebTool` in `tools.ts`) | ✅ Complete |
| 2 | Tool execution (`searchWeb` in `ToolExecutor.ts`) | ✅ Complete |
| 3 | Settings schema + UI (`settings.ts`) | ✅ Complete |
| 4 | System prompt update (`ChatApp.tsx`) | ✅ Complete |
| 5 | Build verification | ✅ Complete |

## Priority
MEDIUM

## Dependencies
- T13 (Agentic Tool Calling) — tool infrastructure
- T9 (Settings) — settings schema and UI

## Completion Criteria

### Tool Definition
- ✅ `search_web` added to `noteTools` export in `src/agent/tools.ts`
- ✅ Zod schema: `query` (string, required), `limit` (number, optional, default 5, max 20)
- ✅ Description guides LLM to use for recent events, news, or facts beyond training data

### Tool Execution
- ✅ `searchWeb()` method in `ToolExecutor.ts`
- ✅ `searchBrave()` — calls Brave Search API v1
- ✅ `searchDuckDuckGo()` — scrapes HTML results (no API key)
- ✅ `searchSearXNG()` — calls JSON API on self-hosted instance
- ✅ Results formatted as markdown: numbered list with title, URL, snippet
- ✅ Error handling with provider name in error message
- ✅ `stripHtml()` helper for DuckDuckGo HTML parsing

### Settings
- ✅ `WebSearchProvider` type: `"brave" | "duckduckgo" | "searxng"`
- ✅ `webSearchProvider` field in `ObsidianAISettings` (default: `"duckduckgo"`)
- ✅ `braveApiKey` field (password input)
- ✅ `searxngUrl` field (text input)
- ✅ `renderWebSearch()` settings section with conditional fields
- ✅ Dropdown: DuckDuckGo (free) / Brave (requires key) / SearXNG (self-hosted)

### System Prompt
- ✅ `search_web` added to tool list in `buildSystemPrompt()`
- ✅ Description: "Use when the user asks about recent events, news, or facts that may have changed since your training data"

### Wiring
- ✅ `ToolExecutor` constructor now accepts optional `settings` parameter
- ✅ All `new ToolExecutor(plugin.app)` calls updated to `new ToolExecutor(plugin.app, plugin.settings)`
- ✅ `toOpenResponses.ts` auto-includes `search_web` via `noteToolsToOpenResponses()`

## Files Changed

| File | Lines | Purpose |
|------|-------|---------|
| `src/agent/tools.ts` | +18 | `searchWebTool` definition |
| `src/agent/ToolExecutor.ts` | +217 | Search execution + 3 providers |
| `src/settings.ts` | +71 | Settings schema + UI |
| `src/components/ChatApp.tsx` | +9 | Pass settings, add to system prompt |

## Notes

- DuckDuckGo scraping may break if they change HTML layout. Brave API is more reliable.
- SearXNG requires a running instance. Good for privacy-conscious users.
- The `provider` field was removed from `ToolResult` return to avoid type error (`provider` not in `ToolResult` interface).
