# Task: T48
*Created: 2026-08-19 13:31:15 IST*
*Last Updated: 2026-08-29 17:08:29 IST*

## Task Information
**Title:** Conversation Compaction Mechanism
**Status:** 🔄 ACTIVE
**Priority:** HIGH
**Created:** 2026-08-19 13:31:15 IST
**Started:** 2026-08-23

## Description

Implement token-budgeted model-context management for long conversations. The
current message-count replay resends large system prompts, history, tool results,
and attachments without semantic compaction. The model should receive a compact
history while the user retains the complete local transcript.

The strategy combines a request budget, bounded tool-result replay, canonical
model-history serialization, and periodic replacement of old turns with a
condensed summary.

## Acceptance Criteria

- [x] Compaction triggers from a configurable estimated-history threshold, with a message-count guard.
- [x] Older turns are summarized into a system-prompt context block.
- [x] Recent turns (configurable, default last 4 messages) are kept in full fidelity.
- [x] Summary generation requests key decisions, tool outcomes, and user intent.
- [x] Compaction runs asynchronously and does not block the active chat request.
- [x] Users can see that compaction occurred through a chat notice.
- [x] The implementation is provider-agnostic; live validation used DeepSeek v4 Flash.
- [ ] Tool-call/result pairing remains fully validated across compaction and replay.
- [x] Request builder reserves space for system prompt, tools, current input, response, and tool output.
- [x] Full UI/export history is separated from bounded model replay history.
- [x] Large tool results have token-based replay limits; attachment-aware accounting remains open.
- [x] Provider-reported usage is kept separate from fallback estimates where available.

## Implementation Details

### Compaction Algorithm

```
Before each request and after every N user turns:
  1. Build a token-budgeted request from system prompt, tools, input, attachments, and history
  2. Identify an older valid prefix to summarize: messages[0 : -keepRecent]
  3. Send these turns to a "summarization" LLM call (same provider, fast model)
  4. Build a separate model-history projection containing the summary and the
     newest "keepRecent" messages; do not mutate the persisted display transcript
  5. Preserve the last "keepRecent" turns in full while retaining the full display transcript
```

### Key Design Decisions

- **Summarization model:** Use the current provider's fast/cheap model (e.g., DeepSeek-V4-Flash) rather than the main model to minimize cost of compaction itself
- **Summary format:** Structured markdown with sections: Key Decisions, Tool Results, User Intent, Open Questions
- **Tool call preservation:** Include a note like "Used read_note on [[Physics Notes]], found: ..." rather than full tool call JSON
- **Trigger conditions:** Compaction fires when the estimated model-facing
  history reaches the configured token threshold and there is an older prefix
  beyond the preserved recent-message tail.

### Integration Points

- Hook into `useMessageActions.ts` after each successful assistant response
- Check the estimated model-facing history and the preserved-message guard
- If eligible, fire asynchronous compaction before a later request uses the
  resulting summary
- Keep `messagesRef.current` and the persisted transcript unchanged; attach the
  summary only to the model-facing request projection

## Related Files

- `src/hooks/useMessageActions.ts` — where messages array is built and managed
- `src/context/tokenEstimator.ts` — for payload size estimation to trigger compaction
- `src/context/semanticCompaction.ts` — summary planning and non-destructive projection
- `src/lib/systemPrompt.ts` — compaction summary is injected as a system message
- `src/agent/AgentLoop.ts` — must handle tool context after compaction
- `memory-bank/tasks/T48a.md` through `T48d.md` — implementation subtasks

## Dependencies

- **Depends On:** T6a (Token Counter Accuracy — needed to know when compaction should trigger)
- **Blocks:** None

## Progress Tracking

- 2026-08-19 13:31:15 IST: Task created from DeepSeek cost analysis. Token investigation showed 892K cache hit tokens per request after 10-turn conversations.
- 2026-08-23 15:27:39 IST: T48 implementation merged to `main` through PR #5 as commit `6a205b9`. The slice includes request budgeting, bounded tool-result replay, semantic compaction, configurable settings, asynchronous summarization, and the user-facing compaction notice.
- 2026-08-23 17:05:26 IST: Live validation completed with trigger `8000`, release `4000`, and `preserveRecentMessages=4`. The fourth turn produced the compaction notice; the follow-up recovered all four test markers and requested requirements.

## Issues and Blockers

- **Protocol-level duplication discovered 2026-08-25:** The OpenResponses loop
  resubmits the full original input on every tool round (`T60b`). This is
  independent of compaction — even with perfect budgets, the transcript
  duplicates itself 17× during a 17-step chain. The fix is in T60b; until
  then, compaction only partially mitigates the symptom.
- Exact older-message retrieval, persisted compaction metadata/summary
  inspection, schema/provenance auditing, repeated-compaction validation, and
  complete tool-pair acceptance remain open. These are tracked primarily
  under T48c; usage reconciliation remains under T48d.

## Notes

- This works with DeepSeek's stateless API (confirmed: they do NOT support `previous_response_id`)
- OpenAI users with Responses API could alternatively use stateful sessions, but compaction is provider-agnostic and benefits everyone
- Estimated savings: ~80% token reduction on long conversations (turns 6-20+ summarized)
- The summarization call itself costs money — need to ensure compaction is cheaper than sending the full history

## Architecture Review Update — 2026-08-29

The fresh read-only architecture review confirms that model-history policy is
the top remaining architecture concern. T48 and T48a through T48c, together
with T62a, remain the policy owners. The next plan is to expose one model-ready
history projection so `TurnLifecycle`, `AgentLoop`, and `OpenResponsesLoop` do
not apply overlapping replay, budgeting, truncation, or compaction rules.

No new task or subtask was created. Exact retrieval, inspectable compaction
metadata, pairing through compaction, and provider/plugin acceptance remain
open as already recorded above.
