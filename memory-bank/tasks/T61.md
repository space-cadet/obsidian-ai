# T61: Self-Settings Agent Tools

## Status

**COMPLETE** — implementation and integration acceptance pass. Priority: HIGH

**Last Updated:** 2026-08-29 10:49 IST

## Description

Give the AI agent the ability to read and modify its own plugin settings via tools, gated behind a "Developer mode" toggle.

## Motivation

During token usage investigation, the AI couldn't see its own configuration (e.g., `maxContextMessages`, `maxToolResultTokens`). A `read_settings` tool would let it self-diagnose issues. A `update_setting` tool (developer mode only) would let power users adjust behavior mid-conversation.

## Proposed Tools

### `read_settings`
- Returns sanitized plugin settings
- **Redacts**: `apiKey`, `password`, `secret`, `auth` fields, provider profile credentials
- **Includes**: token budgets, toggles, file paths, feature flags

### `update_setting`
- Only available when `developerMode === true` (new setting)
- **Mutable whitelist**: `maxContextMessages`, `maxToolResultTokens`, `enableAgentTools`, `autoApply`, `showFullRequestTokens`, `pressEnterToSend`, `autoNameSessions`, `messageHistory`, `includeActiveNote`
- **Immutable**: All credentials, provider profiles, API keys, file paths
- Validates type/range before applying
- Saves settings automatically

## UI Changes

Add "Developer mode" toggle to Advanced settings section. When enabled:
- Registers `read_settings` and `update_setting` tools
- Shows a warning that AI can modify settings

## Security Considerations

- Never expose credentials via `read_settings`
- Audit log all `update_setting` calls
- Confirm destructive changes with user

## Dependencies

- None (builds on existing tool registry)

## Acceptance Criteria

- [x] `read_settings` tool returns sanitized settings
- [x] `update_setting` tool works when developerMode enabled
- [x] `update_setting` rejected when developerMode disabled
- [x] Credentials never exposed
- [x] Tests for both tools

## Current implementation record — 2026-08-29

- `read_settings` and `update_setting` are registered in the shared tool list.
- Settings are recursively sanitized before they are returned.
- Updates use a whitelist, validate the value type, require developer mode,
  save the settings, and write an audit record.
- The Advanced settings section includes the developer-mode toggle.
- The remaining acceptance work is focused integration coverage for both tool
  calls, the disabled-mode rejection, credential redaction, and audit behavior.
- No separate confirmation prompt is implemented; developer mode is the
  current user-controlled gate for setting changes.

## Verification Update — 2026-08-29

Added integration tests through `ToolExecutor` for sanitized reads, the
Developer mode availability gate, valid updates, saving, audit logging,
invalid values, and immutable keys. The full local suite passes: 44 test files
and 373 tests. TypeScript, formatting, and the production build also pass.

The user-controlled Developer mode toggle remains the approval boundary for
setting changes; no separate confirmation prompt was added.
