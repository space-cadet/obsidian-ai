# Task: T71
*Created: 2026-09-19 19:37:00 IST*
*Last Updated: 2026-09-19 19:37:00 IST*

## Task Information
**Title:** Chat View Scroll Performance and Streaming Follow
**Status:** ✅ COMPLETED
**Priority:** HIGH
**Created:** 2026-09-19 19:37:00 IST
**Started:** 2026-09-19 00:00:00 IST
**Completed:** 2026-09-19 19:37:00 IST

## Description

Fix chat-view scrolling regressions and performance on large sessions:
memoized rendering, follow-scroll during streaming, and the send-path race
that released follow mode. Ships on branch `perf/chat-scroll-settings-lag`
(alongside T24 index-only startup work).

## Acceptance Criteria

- [x] Message list renders without per-keystroke re-layout lag (memoized rows, `content-visibility`).
- [x] Streaming replies follow the bottom; user scroll-up interrupts follow.
- [x] Sending a message no longer jumps to a stale saved scroll position.
- [x] Mention pills render for every context item (root cause: only first match per text node replaced).
- [x] Large-session fixture exists for preview/tests; preview build repaired.
- [x] Regression tests cover follow-scroll behavior.

## Implementation Details

Commits on `perf/chat-scroll-settings-lag` (in order):

- `e32eb88` — repaired broken preview build; added large-session fixture.
- `ff3f960` — chat scroll lag + settings collapse lag: memoized message rows,
  `content-visibility: auto`, debounced persistence.
- `7ecccdd` — follow-scroll during streaming with scroll-up interrupt.
- `5443c4e` — mention pills for every context item; trimmed redundant Context footer.
- `fa44b6f` — **send-scroll follow fix**. Root cause: restore-position effect in
  `ChatMessages.tsx` listed `checkScrollPosition` (recreated each `messages.length`
  change) as a dep; on each new message the stale saved `scrollTop` re-applied via
  rAF after the follow effect. Fix: read `checkScrollPosition` through
  `checkScrollPositionRef`; restore deps now `[sessionId, restoreScrollTop]`.
  Red-first test: `src/components/__tests__/ChatMessages.followScroll.test.tsx`.

## Related Files

- `src/components/ChatMessages.tsx` — restore-position effect, memoization.
- `src/components/__tests__/ChatMessages.followScroll.test.tsx` — regression test.
- `src/hooks/useChatUI.ts` / scroll state — follow/interrupt wiring.
- `src/settings-sections/*` — settings collapse lag (part of `ff3f960`).

## Notes

Suite grew 499 → 506 across the branch (T71 + T24 work). Branch merged to main
pending Deepak's device verdicts, then v1.6.0.
