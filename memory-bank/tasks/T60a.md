# Task: T60a
*Created: 2026-08-25 12:52:36 IST*
*Last Updated: 2026-08-26 03:23:54 IST*

## Task Information
**Title:** Canonical Tool Registry and Dynamic Exposure
**Status:** 🔄 Active — integration required
**Priority:** HIGH
**Created:** 2026-08-25 12:52:36 IST

## Description
Connect the typed capability descriptor to the existing execution path. The
adapter and projections from commit `68dc915` are only the starting point;
the registry must be consumed by provider resolution and `ToolExecutor`.

## Acceptance Criteria
- [x] Built-in and provider tools resolve through one collision-checked registry.
- [x] `registry.byId.get('read_note').execute(call)` has a tested execution
  contract equivalent to the existing `ToolExecutor` result.
- [x] `ToolExecutor` delegates through the registry instead of adding another
  name-based dispatch branch.
- [ ] Provider resolution preserves complete definitions until execution.
- [x] Unavailable tools are omitted before the model request.
- [ ] Static prompt and preview branching are replaced by descriptor-driven output.
- [ ] AI SDK and OpenResponses serializers consume the same resolved definitions.
- [ ] Provider schemas receive a basic object-like validity check, and
  availability is resolved through one shared context-aware filter.
- [x] Tests check behavior rather than an exact tool count or boilerplate
  version value.

## Dependencies
- **Depends On:** T60, T39a
- **Blocks:** T60b, T60c, T17 mutation delivery

## Progress Tracking
- 2026-08-25 12:52:36 IST: Planning record created; implementation had not
  started at that point.
- 2026-08-25 13:32:14 IST: Commit `68dc915` added the bounded registry adapter,
  provider normalization, availability filtering, OpenResponses projection,
  and focused tests. Registry-to-execution integration remains open.
- 2026-08-25 13:47:27 IST: Review made the integration test the first gate;
  T60a is not complete until execution consumes the registry.
- **2026-08-25 15:16 IST: Switch fallback removed — registry is sole dispatch path.**
  - Deleted 134-line switch statement from `ToolExecutor.execute()`.
  - All 24 built-in tools now dispatch exclusively through `builtInRegistry`.
  - `execute()` flow: provider tools → `registry.byId.get(toolName)?.execute()` → unknown tool error.
  - Constructor comment updated to reflect T60b status.
- Commit `f72d704`: 295 tests passing (35 test files).
- 2026-08-26 03:23:54 IST: T60a changes were included in `a8e65b7`; review
  fixes for integer parameters and standard/provider JSON schemas landed in
  `bf02760` and `c5f5306`. Remaining descriptor/serializer criteria stay open.
