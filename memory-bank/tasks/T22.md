# T22 — ChatApp.tsx Component Decomposition
*Created: 2026-05-28 23:05 IST*
*Status: 🔄 IN PROGRESS*

## Problem Statement
`src/components/ChatApp.tsx` has grown to **1,948 lines / 63 KB**. It contains:
- 24+ `useState` declarations
- 10 `useRef`s
- 8+ `useEffect` hooks
- ~25 handler functions
- ~300 lines of JSX

This violates single-responsibility principle and makes the component unmaintainable.

## Root Cause
Every chat feature (sessions, streaming, context, group chat, tool approval, editing, export, zen mode, debate mode, auto-save, title generation) is crammed into one component. State, logic, and UI are not separated.

## Refactoring Plan

### Phase 0: Inline Utilities ✅ COMPLETE
Move standalone helper functions out of ChatApp.tsx into `src/lib/`.
- **Files created:**
  - `src/lib/systemPrompt.ts` — `buildSystemPrompt` (67 lines)
  - `src/lib/sessionTitle.ts` — `generateSessionTitle` + `generateSessionTitleLLM` (137 lines)
  - `src/lib/slashCommand.ts` — `SlashCommand`, `parseSlashCommand` (23 lines)
  - `src/lib/contextUtils.ts` — `contextItemKey`, `sameContextItems` (26 lines)
  - `src/lib/agentVisuals.ts` — `getAgentColor`, `getAgentIcon` (28 lines)
  - `src/lib/sessionUtils.ts` — `makeId`, `pruneSessions` (24 lines)
- **Result:** ChatApp.tsx 1,948 → 1,700 lines (-248)
- **Build:** ✅ Passes

### Phase 1: Extract `useChatSession` Hook ✅ COMPLETE
Move all session management (CRUD, active session tracking, auto-save, title generation, persistence) into a dedicated hook.
- **File created:** `src/hooks/useChatSession.ts` (317 lines)
- **Scope extracted:**
  - `sessions`, `activeSessionId`, `chatDataLoaded` state
  - `sessionsRef`, `activeSessionIdRef`, `llmNamedRef`, `saveTimerRef`
  - Load persisted sessions on mount
  - Debounced auto-save effect
  - Auto-title effect (LLM + heuristic fallback)
  - `createNewSession` — with pruning, empty-session dedup, auto-naming previous
  - `deleteSession` — with active-session switch fallback
  - `renameSession`
  - `updateSessionMessages`, `updateSessionContextItems`
  - `manualRenameActiveSession` — returns applied title
  - `autoNameSessions` state + setter
- **ChatApp wiring:** Replaced inline state with hook, updated `handleNewChat`, `handleRenameSession`, `handleToggleAutoName`, `handleManualRename`
- **Result:** ChatApp.tsx 1,700 → 1,533 lines (-167)
- **Build:** ✅ Passes

### Phase 2: Extract `useChatUI` Hook ✅ COMPLETE
Move all UI/side-effect state into a single hook.
- **File created:** `src/hooks/useChatUI.ts` (264 lines)
- **Tests created:** `src/hooks/useChatUI.test.ts` — **31 unit tests** (vitest + @testing-library/react)
- **Test coverage:**
  - Modal open/close (session picker, export, context picker)
  - Zen mode toggle
  - Debate mode toggle
  - Thinking display toggle
  - Participant selection (add/remove/multiple/replace)
  - Dropdown click-outside behavior
  - Typing agent add/remove/clear
  - Editing start/cancel
  - Attachment state
  - Auto-approve toggle
  - Full UI state reset
- **Scope extracted:**
  - All modal states (`showSessionPicker`, `showExportModal`, `showContextPicker`)
  - `zenMode`, `debateMode`, `showThinking`
  - `selectedProfileIds`, `showParticipantDropdown`, `participantDropdownRef`
  - Click-outside effect for participant dropdown
  - `typingAgents`
  - `isEditing`, `originalMessages`, `editMessageText`
  - `messageAttachments`
  - `autoApprove`
- **ChatApp wiring:** Replaced inline state with `const ui = useChatUI()`, updated all JSX references and handler functions
- **Result:** ChatApp.tsx 1,533 → 1,483 lines (-50)
- **Build:** ✅ Passes
- **Tests:** ✅ All 31 pass

### Phase 3: Extract `useMessageActions` Hook ✅ COMPLETE
Move all message action handlers into a dedicated hook.
- **File created:** `src/hooks/useMessageActions.ts` (1,111 lines)
- **Tests created:** `src/hooks/__tests__/useMessageActions.test.ts` — **21 unit tests**
- **Scope extracted:**
  - `handleSend` (~664 lines) — group chat + single chat (agent, tool, standard)
  - `handleStop`
  - `handleRetry`
  - `handleEditMessage`, `handleCancelEdit`
  - `handleAppend`, `handleInsertAtCursor`, `handleApply`
  - `handleApplyToTarget`, `handleCreateNote`, `handleAppendToTarget`
  - `handleApproveTool`, `handleRejectTool`
- **ChatApp wiring:** Replaced all inline handlers with `const actions = useMessageActions(deps)`, updated all JSX references
- **Result:** ChatApp.tsx 1,483 → **636 lines** (-847, -57%)
- **Cumulative reduction:** 1,948 → 636 lines (-1,312, -67%)
- **Build:** ✅ Passes
- **Tests:** ✅ All 52 pass (31 + 21)

### Phase 4: Extract Session/Settings/Export Handlers ⏳ PENDING
Move remaining handlers into dedicated hooks.
- **Target files:**
  - `src/hooks/useSessionActions.ts` — `handleNewChat`, `handleLoadSession`, `handleDeleteSession`, `handleRenameSession`
  - `src/hooks/useSettingsActions.ts` — `handleToggleAutoApprove`, `handleToggleAutoName`, `handleManualRename`
  - `src/hooks/useExportActions.ts` — `handleExportChat`
- **Estimated ChatApp:** 636 → ~400 lines

### Phase 5: Extract `ChatLayout` Sub-Components
Break the JSX monolith into smaller presentational components.
- **Target files:**
  - `src/components/ChatLayout.tsx` — main layout shell (zen mode, sidebar)
  - `src/components/ChatToolbar.tsx` — action bar + participant dropdown
  - `src/components/ChatMainArea.tsx` — messages + input + tool cards
- **Scope:** Move JSX only; keep logic in hooks above.

## Target Outcome
| File | Before | After |
|------|--------|-------|
| `ChatApp.tsx` | 1,948 lines | **636 lines** (target: ~300) |
| `useChatSession.ts` | — | **317 lines** ✅ |
| `useChatUI.ts` | — | **264 lines** ✅ |
| `useMessageActions.ts` | — | **1,111 lines** ✅ |
| `src/lib/*.ts` (6 utils) | — | **305 lines** ✅ |

## Decisions

### Single Hook vs. Multiple Hooks
Extracted ALL message action handlers into a single `useMessageActions` hook rather than splitting into `useSendActions`, `useNoteActions`, `useToolActions`, etc. Rationale:
- These handlers share the same dependency surface (plugin, orchestrator, session refs, controller ref, etc.)
- Splitting would create 4–5 hooks each needing the same 20+ parameters
- One hook keeps the call site in ChatApp clean: `const actions = useMessageActions(deps)`
- Future: if the hook grows beyond 1,500 lines, reconsider splitting by domain

### Test Strategy
- Wrote tests for simpler handlers first (note actions, editing, stop, tool approval)
- Basic test for group chat path of `handleSend`
- Deferred comprehensive `handleSend` testing (AgentLoop, OpenResponsesLoop, standard streaming) — mock setup would be 200+ lines for marginal value
- `handleRetry` tests mock `handleSend` rather than testing the full resend flow

## Open Questions
1. Should `useMessageActions` be split into `useSendActions` + `useNoteActions` + `useToolActions`? (see Decisions above — defer until hook >1,500 lines)
2. Should `handleSend` accept a `getHistory()` callback instead of reading `messagesRef` directly? Would make testing easier but adds indirection.

## Commits
- `56caaa7` refactor(T22): Phase 0 + Phase 1 — extract utilities + useChatSession hook
- `d57a219` refactor(T22): Phase 2 — extract useChatUI hook with tests
- `49fbdc7` docs(memory-bank): record T22 Phase 2 completion
- (pending) refactor(T22): Phase 3 — extract useMessageActions hook with tests
