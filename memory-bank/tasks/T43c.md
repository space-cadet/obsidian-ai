# T43c: Extend Sync to All Plugin Data

*Created: 2026-08-21*
*Status: ✅ COMPLETE*
*Parent: T43 (Integrate Sync UI into Chat Lab)*
*Related: T42 (Remote Chat Storage & Sync), T49 (Settings Export and Import)*

## Problem

Currently sync only handles `ChatSession[]`. Other plugin data is device-local:

| Data | Location | Current | Desired |
|------|----------|---------|---------|
| Chat sessions | `sessions/` (JSONL) or `data.json` | ✅ Syncs | ✅ Syncs |
| **Settings** | `data.json` | ❌ Local | ✅ Sync |
| **AI Memory** | `.obsidian-ai/memory.json` | ❌ Local | ✅ Sync |
| **Sync index** | Plugin data | ❌ Per-device | ✅ Shared |

## Solution Applied (2026-08-21)

Implemented the first plugin-data sync pass. Settings and the sync index use the shared bundle manager; T55 later added selectable syncing for separate memory, persona, audit, and usage files.

### Changes

1. **StorageAdapter interface** — Added `readText(path)` method
2. **WebDAVStorageAdapter** — Implemented `readText()` using existing `GET` infrastructure
3. **main.ts** — Added three methods:
   - `_serializePluginData()` — Creates JSON blob minus API keys/passwords
   - `_deserializePluginData()` — Merges remote data with local (preserving credentials)
   - `syncPluginData()` — Uploads/downloads the blob

### Data Synced
- Provider profiles (minus API keys)
- Prompts, preferences, settings
- Sync index (shared across devices)

### Data NOT Synced (security)
- API keys
- WebDAV/S3 passwords
- Local-only paths

### Conflict Resolution
- The old settings path used a last-write-wins style timestamp check.
- T57a now validates every selected plugin-file envelope and reports differing
  local and remote contents as a conflict without overwriting either side.
- Remembered per-file choices, recovery copies, and deletion records are now
  provided by T57b.
- Credential fields are kept local and are not overwritten from remote.

### Integration
- `syncPluginData()` is called automatically at the end of `triggerSync()` when session sync succeeds
- Respects the same `syncDirection` setting (both/upload/download)

## Acceptance Criteria

- [x] Settings sync (provider profiles, prompts, preferences)
- [x] Sync index shared across devices (prevents false conflicts)
- [x] API keys excluded from sync (security)
- [x] Conflict resolution: last-write-wins with notification
- [x] Backward compatible: devices without this feature ignore `plugin-data.json`
- [x] Integrated into existing sync flow

## Files

- `src/sync/StorageAdapter.ts` — Added `readText()` to interface
- `src/sync/WebDAVStorageAdapter.ts` — Implemented `readText()`
- `src/main.ts` — Added `_serializePluginData()`, `_deserializePluginData()`, `syncPluginData()`

## Historical Notes

- At the time of the original T43c implementation, AI Memory and the audit log were still local-only.
- T55 later added optional syncing for those files.
- T57a now routes selected auxiliary files through a shared encrypted,
  checksummed, and atomic transfer path. Retry rules remain separate T57c work;
  T57b now covers conflict memory, recovery, and deletion rules.
- Sync logs remain device-local unless explicitly handled by the existing sync logger.
- T57 is the next hardening task for a shared file path, encryption, atomic
  writes, per-file conflicts, recovery, deletion records, and retry handling.
