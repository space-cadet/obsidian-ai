#### 21:58:00 IST - T66: generated-stylesheet finding recorded (styles.css build artifact, T42g block → styles/_sync.css)

**Action:** Modified
**Files:**
- Modified: styles/_sync.css (T42g sync-panel block relocated here from the generated file; 7 `sync-panel` selector families — working tree, uncommitted)
- Modified: styles.css (regenerated by scripts/concat-styles.mjs; full 6219-line output with the T42g tail intact — working tree, uncommitted)

Root cause of the recurring "vanishing T42g CSS" (14:29 UTC incident,
~544-line tail loss): `styles.css` is GENERATED at build time by
`scripts/concat-styles.mjs` from the `styles/_*.css` partials — every
`pnpm run build` rewrites it and silently discards hand-appended CSS.
Correct fix applied in working tree 15:17 UTC by an in-session actor (not
Cloudy). Per Deepak 15:38 UTC ("don't worry about the css files for now")
both files intentionally left uncommitted by Cloudy. Standing rule: edit
the partials, never the generated file.
